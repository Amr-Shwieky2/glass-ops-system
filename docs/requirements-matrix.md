# Sprint 0 Requirements Matrix — Glass Operations Management System

## Sprint 3 changelog (2026-09-14) — AI Hebrew quote + signing compliance

Verified against master execution prompt sections 5 (A1–A5) and 8 (G1–G7). Two real fixes shipped and verified; one attempted, tested, and deliberately reverted with evidence — reported honestly rather than claimed.

**Fixed and verified:**
- **G1 — mandatory signing fields**: `SignSchema` (`src/server/quotes/actions.ts`) now requires non-empty phone, national ID/company ID, and installation address server-side (previously only name/terms/signature were required, contradicting the "mandatory" framing). The public signing form's labels were quietly saying "(optional)" for national ID — relabeled to `*` required, matching name; the submit button is now also disabled client-side until all four text fields are filled, not just signature+terms. All 6 e2e helpers that sign a quote were updated to fill national ID and address (previously blank, silently accepted).
- **G7 — signed PDF must render exactly what was signed**: `getQuoteRenderData` (the internal, permission-gated `/api/quotes/[quoteId]/pdf` route) previously always rendered `quotes.currentVersionId` — so once a signed quote was revised into a v2 draft, an employee opening "the quote's PDF" silently saw the new, unsigned draft with no indication it wasn't what the customer signed. Now renders `signedVersionId ?? currentVersionId`. New e2e assertion added (though PDF content itself can't be asserted — Chromium's PDF output compresses its content streams, so the check is necessarily at the response/DB level, documented inline).

**Attempted and reverted (A1 — enrich the AI prompt with structured job data + company terms):** implemented, then live-tested against the actual configured model (`qwen2.5:3b` via local Ollama) with the real prompt shape, several times. Result: adding customer name / job title / existing-items summary — even without the company-terms addition — measurably degraded output reliability on this specific small model (echoed prompt labels back as content, mixed-language garbage, and in one run an empty `quantity` field that fails schema validation outright, reproducing the exact "تعذر إنشاء المسودة" failure a user would hit). The *baseline* prompt (unchanged) was independently re-verified in the same test session to still produce valid, schema-conforming JSON. Shipping the enrichment would have made this feature worse, not better, on the model this deployment actually runs — reverted `src/server/ai/quote-generator.ts` and `src/server/quotes/ai-draft-actions.ts` to their pre-Sprint-3 state (verified via `git diff`, zero remaining changes). This is a genuine model-capacity limit, not a prompt-wording bug: a larger model (`OLLAMA_MODEL=qwen2.5:7b+`, already a one-line config change with no code change) would likely handle the added context fine, but that's an operator/infrastructure decision, not something to force through code. A1 remains PARTIAL, unchanged from Sprint 0.

**Not touched in Sprint 3** (real, tracked gaps): G4 (no stamp pad/upload for company customers), G6 (signing-time address confirmation is captured but never propagated to `jobs.address` or shown to staff — dead data), A2–A5 were already IMPLEMENTED per Sprint 0 and untouched. P5 (a signed quote's displayed item description is a live join to mutable `work_types.labelAr`, not a frozen snapshot) and the dead quote `notes` field (R1.20/R1.52) are adjacent quote-integrity issues surfaced by the same audit but out of this sprint's G1/G7/A1 scope — left for a future pass.

**Verification**: lint clean · typecheck clean · unit 133/133 · Playwright 30/30 · production build succeeds.

## Sprint 2 changelog (2026-09-14) — field measurement quick-submit completeness

Verified against the master execution prompt section 4 checklist (Q1–Q8) and the project's own design spec (`docs/superpowers/specs/2026-09-03-new-measurement-quick-submit-design.md`). Most of Sprint 2 was already IMPLEMENTED per Sprint 0 (Q1/Q2/Q3/Q5/Q7/Q8, S2.1) — this sprint closes the two named gaps plus the testing gap:

- **Q4 — glass type is its own admin-editable list**: added `createGlassTypeAction`/`updateGlassTypeAction` (mirroring the existing work-types CRUD pattern) and a new "أنواع الزجاج" tab on `/settings`. Verified live in the browser: create, list, and the auto-closing dialog all work. Closes Q4.
- **Q6 — persisted submission source**: new `measurement_source` enum column on `measurements` (migration `0009`), defaulting to `'office'`, set to `'field_quick_submit'` by `submitFieldMeasurementAction`. Previously this signal only existed as an inference over other columns and was never durably queryable. Closes Q6.
- **S2.2 — the design spec's own missing 3rd Vitest area**: extracted the reference-price + VAT-label display formatting (previously inline JSX in two places) into a pure `formatFieldQuotedPrice` helper (`src/lib/field-quoted-price.ts`), with 6 new unit tests. Also strengthened the existing Playwright test to assert the actual rendered price figure and VAT label text, not just that a selector was clicked. Closes S2.2.

**Verification**: lint clean · typecheck clean · unit 133/133 (was 127) · Playwright 30/30 · production build succeeds · migration applied and reseed verified.

**Not touched in Sprint 2** (out of this sprint's scope, tracked for later): whether `fieldQuotedPrice` should itself be gated behind a financial permission the way the agreed sale price now is — it wasn't named in either audit as a leak and gating it could block the very technician who entered it from seeing their own reference figure; left as a judgment call for a future sprint if you want it revisited.

## Sprint 1 changelog (2026-09-14) — security & authorization hardening

The rows below this line are the **Sprint 0 snapshot**, unedited — kept as the historical baseline. The items listed here were fixed in Sprint 1; their Sprint-0 row is now stale on these specific points. Full detail in the Sprint 1 report delivered to the user; summary:

**Fixed and verified (30/30 E2E, 127/127 unit, lint/typecheck/build clean):**
- New `VIEW_SALE_PRICE` permission (granted to Amr/Mohammad/Issam, not Basel); sale price now gated on the job page (header, items card, per-item, `QuoteSection`'s line items/total), jobs list, customer page, and the internal quote PDF route. Closes I6, P4 (sale-price half), R1.41, S1.4, S1.5, T4, RG13.
- `ProductionSection`'s factory-price render now gated behind `VIEW_JOB_COSTS`/`APPROVE_FACTORY_PRICE`. Closes R1.10.
- `/customers` list: added the missing `VIEW_CUSTOMERS` gate; both it and `/customers/[id]` now scope to job-involvement (or `VIEW_ALL_JOBS`/`CREATE_CUSTOMER`) for a restricted viewer — the live-demonstrated national-ID leak is closed. Closes S1.1, R1.15.
- `/repairs` list scoped the same way (plus a `responsibleUserId` fix so a technician sees repairs they're personally responsible for). Closes R1.32, and the R1.35 dashboard sub-gap.
- `completeInstallationAction`: added assignee-ownership check, a `COLLECT_PAYMENT` check before recording a payment, and a `job_id` filter on the job-items update. Closes CRITICAL finding #1.
- `approveFactorySubmission`/`rejectFactorySubmission`: now derive `jobId` from the submission's own request (mismatch is a hard error) and lock the submission row before the conditional approve/reject update. Closes CRITICAL finding #3, downgrades R1.29 back toward IMPLEMENTED.
- `createMeasurement`: added a job-visibility check before trusting the caller-supplied `jobId` — closes the self-grant-visibility hole. Partial progress on S1.2 (other listed actions in S1.2/S1.3 are NOT yet fixed — see below).
- `cancelAppointmentAction`: derives the real job from the appointment row, adds an assignee-or-scheduling-permission check, and a race-safe conditional status update.
- Login: added a rate limiter (`src/server/security/rate-limit.ts`), keyed by phone and by IP, counting only failed attempts. Public quote-PDF/signing and factory-submission endpoints also rate-limited. Closes S1.7 (rate-limiting half of S1.8).
- Self-approval prevention (master prompt section 9): new `isSuperAdmin`/`requesterMayApprove` helpers; wired into customer-payment decide, job-cost create+decide, technician self-reported-payment create+decide, field-expense decide, and cash-transfer confirm — plus the matching UI decision buttons now hide for the requester (not just server-side rejection). Deliberately NOT applied to factory-submission approve/reject (the submitted price comes from an unauthenticated external factory, not a company requester — see the code comment in `production/approve.ts`). Closes V1, V2, S1.6.
- New regression test (`permissions.spec.ts`, "an assigned installer with no VIEW_SALE_PRICE cannot retrieve the sale price") asserting on raw HTTP response bodies, not rendered UI — it caught the `QuoteSection` leak above before this changelog was written. Progress on S1.9 (not yet exhaustive — no test yet for the customer-PII fix or the repairs-list fix).

**Explicitly NOT done in Sprint 1 (real, tracked gaps — do not treat as fixed):**
- S1.2/S1.3: `scheduleAppointmentAction`, `addPaymentAction`, `cancelJob`, `closeJobAction`, `sendQuoteAction`, `deleteJobItem`, `removeAssignment`, `convertQuoteToJob` still trust a caller-supplied `jobId`/child-id without an ownership/visibility check.
- S1.8: factory links still never expire and have no revoke action (columns exist, unused) — only the rate-limiting half of S1.8 was addressed.
- D5: `.env.example` still not tracked in git (deliberately deferred to whichever sprint touches deployment docs — out of Sprint 1's scope, not forgotten).
- No test yet directly targets the self-approval-prevention behavior at the HTTP/DB level (S1.9's "no test proves self-approval prevented" gap) beyond the e2e tests that were updated to exercise the new pending-then-different-approver flow as a side effect.

---

# Sprint 0 Requirements Matrix — Glass Operations Management System

Synthesized from 9 domain verifiers, 9 domain skeptic refuters, 2 live HTTP probe sessions, and one live Playwright E2E run (this session, 2026-09-14). Two checklist files referenced by the task brief (`requirements-checklist.md`, `sprint0-checklist.md`) and a prior `coverage-synthesis.md` were **not present** in the scratchpad at synthesis time (confirmed absent by two independent domain agents after repeated `find`/`ls` passes). Item wording below is reconstructed from each verifier's own quoted requirement text; item **statuses and evidence are unaffected** by the missing files since every verifier independently re-derived requirement intent from source and live testing.

## (a) Status vocabulary and evidence rules

| Status | Meaning |
|---|---|
| **IMPLEMENTED** | Requirement fully met: server-enforced, UI-wired, and (where claimed) test-covered. No open sub-gap found by any verifier, refuter, or probe. |
| **PARTIAL** | Core mechanism exists and works for the common path, but a named sub-requirement, edge case, or scope is unmet (e.g., feature works but isn't tested; permission exists but one surface bypasses it). |
| **MISSING** | No code path implements the requirement (schema may exist, but nothing reads/writes it, or no UI/action exists). |
| **SECURITY ISSUE** | A verifier, refuter, or live probe demonstrated (via source trace to a reachable, unauthenticated-or-under-privileged code path, or via live HTTP proof) that unauthorized data is retrievable or an unauthorized mutation is possible. |
| **BROKEN** | Code exists and is reachable but produces incorrect behavior on a realistic path (not a security boundary issue), or a documented setup step fails from a fresh checkout. |
| **SCHEMA ONLY** | Database columns/tables exist for the capability but zero application code path reads or writes them. |
| **DATA INTEGRITY** | (finding-level tag, not a top-level status) — a defect that corrupts stored financial/operational data without necessarily being an authorization bypass. |

**Reconciliation rules applied (per task instructions):**
1. Where a refuter marked `refuted: true` against an IMPLEMENTED domain verdict, the refuter's `newStatus`/`reason` replaces the domain verdict (12 items changed this way; 2 of them are *escalations* to SECURITY ISSUE/BROKEN, not downgrades to PARTIAL).
2. Where two domains rated the same item (only V1/V2, rated MISSING by both `auth-security` and `finance`), the stricter status is kept and evidence merged.
3. Where a live probe finding contradicts or is not yet reflected in a verdict, the probe wins and the item is escalated to SECURITY ISSUE (applied to S1.7, R1.15; corroborating-only probe evidence is folded into existing SECURITY ISSUE items without further escalation).
4. One item (R1.58) was refuted for citing a non-existent evidence file, but this session's own **live E2E run** (29/29 passed, see §f and the Sprint Status doc) independently supersedes that specific concern; R1.58 is reported IMPLEMENTED with the refuter's citation defect noted as a documentation-hygiene caveat, not a functional gap.

## (b) Coverage counts by status

| Status | R1.x / R2.x (64 items) | Master-prompt items (117 items) | **Total (181 items)** |
|---|---:|---:|---:|
| IMPLEMENTED | 25 | 49 | **74** |
| PARTIAL | 34 | 38 | **72** |
| SECURITY ISSUE | 4 | 10 | **14** |
| MISSING | 0 | 16 | **16** |
| BROKEN | 1 | 2 | **3** |
| SCHEMA ONLY | 0 | 2 | **2** |
| **Total** | **64** | **117** | **181** |

74/181 (41%) fully implemented; 72/181 (40%) partially implemented with a named gap; 14+16+3+2 = 35/181 (19%) missing, broken, security-issue, or schema-only.

## (c) Requirement checklist — R1.1–R1.58, R2.1–R2.6

| ID | Requirement | Status | Evidence | Gap |
|---|---|---|---|---|
| R1.1 | No paid SaaS required for core functionality | IMPLEMENTED | package.json deps (no stripe/twilio/openai/etc.); ARCHITECTURE.md:8-28 license table; DEPLOYMENT.md:344-358 | None. |
| R1.2 | AI quote drafting not dependent on paid LLM API | IMPLEMENTED | src/server/ai/provider.ts:9-15,100-124 (local Ollama only); .env.example:11-19 | None. |
| R1.3 | No credit card required for V1 core functionality | IMPLEMENTED | DEPLOYMENT.md:271-276,344-358 | None. |
| R1.4 | Fully self-hostable (Docker Compose) | IMPLEMENTED | Dockerfile:1-105 (multi-stage, HEALTHCHECK); docker-compose.yml:1-71 | Not run live in this audit (scope forbids container start/stop); assessed by source read only. |
| R1.5 | Never hardcode behavior by user's name; permissions only | IMPLEMENTED | src/server/auth/permissions.ts:14-20 (`can()` only reads `user.permissions` Set); permissions.test.ts:63-69 (named 'Amr' user w/ 0 grants denied); repo-wide grep for name/id/phone literal branching: none found | Ownership checks elsewhere are correctly id-based (finance/technicians/[userId]/page.tsx:122; notifications-actions.ts:36-40). |
| R1.6 | Money never as JS floats — decimal representation | IMPLEMENTED | src/server/money.ts:11-79 (decimal.js, toFixed(2) strings); DB scan: 0 float/real/money columns among 30 money-bearing numeric(12,2) columns; money.test.ts float-trap regressions (0.1+0.2, 1.005, 30×0.10) | commission_rate_percent divides via a JS float before re-entering Decimal (commission.ts:78) — exact for realistic 2dp percents, not a live defect. |
| R1.7 | Server-side authorization always; UI-hiding is never the gate | PARTIAL | Universal `can()`/`canAny()` in all 30 "use server" files and 6 route handlers (src/server/jobs/actions.ts:53-61 et al.); live unauth probes: /api/notifications, /api/reports/*, /api/attachments/* → 401; /jobs,/customers,/finance → 307 | (1) /customers list page has **no** permission check at all (only nav-hides the link); (2) job-scoped mutations (createMeasurement, scheduleAppointmentAction, addPaymentAction, cancelJob, closeJobAction) check a permission key but never that the caller may see the job; (3) child-entity actions trust client-supplied jobId (see S1.3). |
| R1.8 | Financial totals from transaction rows, never mutable stored totals | IMPLEMENTED | DB scan: no balance/paid/remaining column anywhere; src/server/finance/queries.ts:36-99 (SUM at read time); src/server/payments/queries.ts:63-72; jobs.sale_price_total written only once by quotes/convert.ts:69 | None. |
| R1.9 | Job Details page composes all ~11 functional sections from real queries | IMPLEMENTED | src/app/(app)/jobs/[id]/page.tsx:111-147 (Promise.all of 17 real queries); 11 Card sections (Overview, Measurements, Items, Quote, Production, Costs, Appointments, Repairs, Assignments, Payments, Compensation) each backed by a non-stub component file | Checklist wording unconfirmed (file missing) but substance verified. |
| R1.10 | Job Details sections gated by viewer's actual server-computed permissions | **SECURITY ISSUE** *(refuted from IMPLEMENTED)* | CostsSection/RepairsSection/PaymentsSection/CompensationSection are correctly gated (page.tsx:457,476,526,536); **ProductionSection is not** (page.tsx:449-455) and unconditionally renders `latest.submittedPrice` (factory cost) to any job viewer (production-section.tsx:104-117); live-reproduced logged in as Basel: Costs/Compensation absent, but "آخر عرض من المصنع ‏3,200.00 ₪" visible | One of the 11 sections (Production) leaks a protected cost figure to users without VIEW_JOB_COSTS/VIEW_PROFITABILITY; the project's own permissions.spec.ts never asserts on this card, so the gap is untested. |
| R1.11 | Section action controls wired to real, authorized server actions | IMPLEMENTED | page.tsx:422-427,512-517 bound to real actions matching their own server-side permission checks (jobs/actions.ts:341-348,420-427); repo-wide grep for dead handlers (`onClick={() => {}}`, `href="#"`, bare `alert()`): 0 hits | None found. |
| R1.12 | Job is the central linking object (customer/measurement/quote/items/production/costs/technicians/contractors/appointments/payments/repairs/compensation/profitability/audit) | IMPLEMENTED | src/server/db/schema/jobs.ts:32-90; relations.ts:77-112 (11+ relations); jobs/queries.ts:120-246 getJobDetail; jobs/[id]/page.tsx:111-147 fetches all linked entities in one Promise.all; audit rows tagged entityType='job' from 10+ action modules | Job page has no Activity/audit timeline of its own (audit only visible at /admin/audit-log); job_items has no 'responsible' column (R1.22). |
| R1.13 | Users table (id/name/phone/email/credentials/status/permissions/vehicle/financial-visibility/timestamps); 4 demo personas | IMPLEMENTED | schema/auth.ts:22-111; live DB: 4 users, permission counts عمرو=32/محمد=25/عصام=12/باسل=7 exactly matching seed.ts:83-131 key arrays; admin UI (edit-user-dialog.tsx, permissions-editor.tsx) | Financial visibility is expressed purely via permission keys (correct per spec), no per-user flag column. |
| R1.14 | 29 named granular permissions, independently assignable, enforced server-side | **PARTIAL** *(refuted from IMPLEMENTED)* | 31 of 32 catalogue keys wired end-to-end; admin Permissions editor (permissions-editor.tsx) + grantPermissionAction/revokePermissionAction (users/actions.ts:420-476,501-574) w/ last-holder lockout guard | `edit_measurement` (permission-keys.ts:20,92; granted to محمد/عصام in seed) is **enforced nowhere** — no updateMeasurement action or edit UI exists at all; the permission is purely decorative. |
| R1.15 | Customer profile (name/phone/national-id/address/maps-link/lat-lng/notes); page shows quotes/jobs/payments/checks/balance/repairs/activity | **SECURITY ISSUE** *(escalated by live probe)* | Profile fields present (schema/customers.ts:17-39); page shows only 2 of 8 required sections (profile + job list, customers/[id]/page.tsx:80-168); **Live Probe 1**: logged in as Basel (VIEW_CUSTOMERS + VIEW_ASSIGNED_JOBS only, no job relationship to the customer), GET /customers/0f058ca0-...(Khaled) → 200 OK full profile incl. would-be national ID; GET on Ahmad's customer page returned national ID '300123456' in plain text; reproduced independently for a second user (Issam) against two other customers | getCustomerById (customers/queries.ts:58-64) has **zero** viewer/ownership predicate — any authenticated user can read any customer's PII (incl. national ID) regardless of job involvement; also missing quotes/payments/checks/outstanding-balance/repairs/activity sections; lat/lng cannot be captured by any form. |
| R1.16 | Job page Call/Waze/Maps buttons, no paid API | IMPLEMENTED | src/lib/location-links.ts (plain tel:/waze.com/maps.google.com builders, no key); location-links.test.ts 14/14 pass; grep for GOOGLE_MAPS_API_KEY/mapbox: 0 hits | Buttons fall back to address-text search since no action ever writes jobs/customers.latitude/longitude. |
| R1.17 | Full lead-to-job workflow (29 steps), no duplication between stages | PARTIAL | 15 of 18 seeded statuses reachable by live code; forward-only guard (jobs/status.ts:28-52, unit-tested 4/4); no duplication (quote items copied into job_items once, jobs.source_quote_version_id keeps reference) | 3 statuses (waiting_for_pricing, waiting_for_customer_approval, installation_in_progress) are dead — only seed data ever occupies them; no edit path for measurements/job items/appointments after creation; re-signing a quote wipes+recreates job_items (see §e). |
| R1.18 | Operational status vs payment status separate; 17 configurable statuses; payment status computed as Not/Partially/Fully Paid | PARTIAL | Separate columns (jobs.status_id vs computed remaining, jobs.ts:28-30); 18 seeded statuses, admin CRUD (lookups/actions.ts:88-148,178-269) | No payment-status **label** exists anywhere (grep for "مدفوع"/paymentStatus: 0 hits) — only raw remaining/paid figures render, never classified. |
| R1.19 | Measurements (measured-by/date/info/photos-flag/notes/pricing-responsible); Measured-By/Priced-By/Deal-Closed-By tracked independently | PARTIAL | schema/jobs.ts:166-200 (3 independent FKs); createMeasurement (jobs/actions.ts:173-275) sets measuredByUserId + pricingResponsibleUserId; dealClosedByUserId set at conversion (quotes/convert.ts:78); ahmad-scenario.spec.ts:130-135 asserts independence | `edit_measurement` permission is inert (no edit action exists, see R1.14); single `details` column conflates info+notes; createMeasurement has no job-scope check (self-involvement escalation, see §e). |
| R1.20 | Quote builder (full field set) + PDF + secure public link + customer signs + signed version immutable + edits create new version | **PARTIAL** *(refuted from IMPLEMENTED)* | Builder UI (quote-builder-dialog.tsx); versions.ts:71-180 always INSERTs a new version (never UPDATEs, confirmed by repo-wide grep); signing tx (actions.ts:316-444); quotes.spec.ts proves v1 byte-equal after v2 edit | `notes` field is schema-only — never collected by the builder UI, never persisted, never rendered in the PDF (dead end-to-end); item description on an already-signed quote's PDF/page is a **live join** to the mutable `work_types.labelAr`, not a frozen snapshot — an unrelated MANAGE_SETTINGS admin edit silently changes what a signed quote displays. |
| R1.21 | Convert signed quote to job, transferring customer/items/prices/address/measurements/notes/signed-reference | PARTIAL | convert.ts:36-95 applySignedQuoteToJob transfers items/price/quote-reference/closer; customer/measurements/notes inherent (quotes.job_id NOT NULL); ahmad-scenario.spec.ts:188-204 | Installation address & GPS location the customer confirms at signing (quote_signatures.customer_address_at_signing/lat/lng) are **never copied** to jobs.address/lat/lng — dead data, Waze/Maps buttons keep using the office-entered address; re-conversion wipes+recreates job_items (see §e). |
| R1.22 | Job items (type/desc/qty/unit/price/cost/responsible/status/notes); multi-installer at job AND item level; external contractors as assignees | PARTIAL | job_items schema (jobs.ts:93-120); addJobItem (jobs/actions.ts:289-339); job-level multi-assignment works (repeated assignToJob inserts) | job_items has **no** `responsible` column; item-level assignment (jobAssignments.jobItemId) is schema-only — no UI/action ever populates it (live DB: 0 item-level rows); no update path for job items after creation. |
| R1.23 | Configurable compensation rules (12), custom 'Other', manual unequal allocation, configurable vehicle deduction, daily-wage + overtime/fuel/bonus/penalty | PARTIAL | Exactly 12 default compensation_rules (seed.ts:224-237); admin CRUD (lookups/actions.ts:486-660); allocateInstallationEarning supports rule-based or custom (compensation/actions.ts:82-209) | No overtime enum value or action exists at all; fuel_reimbursement enum value has zero writer; unequal allocation only works by submitting once per participant (no split UI); allocateInstallationEarning doesn't verify jobItemId belongs to jobId nor technician assignment. |
| R1.24 | Technician ledger (earnings/wages/commissions/bonuses/penalties/reimbursements/adjustments/payments-received/balance) from rows; negatives valid; self-report Pending unless approver | PARTIAL | technician_ledger_entries append-only (compensation.ts:24-80); balance = SUM(approved) only, negatives rendered red; reportTechnicianPayment (actions.ts:523-584) hard-codes userId=self, pending unless MANAGE_TECHNICIAN_PAYMENTS | `fuel_reimbursement`/`other_adjustment` enum values have zero writer; **no manager-side action exists to record a company payout to a technician** — only the technician's own self-report can create a payment_made row; a MANAGE_TECHNICIAN_PAYMENTS holder's own self-report auto-approves silently. |
| R1.25 | Configurable penalty rules (5) + bonus rules; penalties visible to worker w/ reason; commission = configurable % of gross profit, estimated vs final | IMPLEMENTED | Exactly 5 penalty rules + 1 bonus rule seeded (seed.ts:238-247); admin CRUD confirmed by refuter as genuinely wired into settings UI (not seed-only); recordPenalty requires `reason` (actions.ts:446), shown on worker's own ledger; commission.ts:57-379 rate from admin setting, race-safe finalize | Estimate is a manual button (no auto-recompute as costs change); each estimate overwrites the prior in place (history only in audit_logs). |
| R1.26 | Customer payments (cash/bank/check/other) w/ approval; personal cash boxes from transactions; handover/transfer w/ confirmation; bank bypasses cash boxes; company balance visible | IMPLEMENTED | payments/record.ts:56-126 (cash credited only when method='cash' AND approved, confirmed by refuter reading both credit sites); transfer-actions.ts (pending→confirm, race-safe conditional UPDATE); finance/technicians/[userId]/page.tsx:110-132 correct isOwnPage/VIEW_TECHNICIAN_BALANCES guard | Neither transfers nor field expenses check the sender's drawer balance first — drawers can go negative; createCashTransfer needs only a login (not COLLECT_PAYMENT). |
| R1.27 | Incoming (6 statuses) + outgoing checks; due-date reminders; status never auto-clears | PARTIAL | Enums confirmed (6 incoming/5 outgoing, enums.ts:26-41); manual-only status transitions (checks/actions.ts:104-108,236-240 exclude auto states) | No reminder **notifications** exist at all (dashboard row + highlight only, no scheduler); due_soon assigned only at creation, never re-evaluated later; status updates are blind UPDATEs with no transition table/race guard; a cleared incoming check never posts a customer_payment (money can be double- or never-entered). |
| R1.28 | Calendar day/week/month, 5 event types, shows time/customer/type/assignees/location, click→job; multi-assignee scheduling; conflict warnings; My Day vs. see-everyone split | PARTIAL | FullCalendar (calendar-view.tsx:90-99) w/ 3 views, direction=rtl; api/appointments/route.ts:32-86 self-vs-all scoping enforced server-side; getAssigneeConflicts (appointments/queries.ts:333-377) | Events show neither assignees nor location (not in the API payload / never rendered); no reschedule/edit action (only cancel+recreate); `locale="ar"` is passed without importing the FullCalendar Arabic locale module, so several UI strings render in English; conflict check ignores 'arrived' appointments; zero automated tests for calendar/conflicts. |
| R1.29 | Production request w/ secure factory public link (no account); factory submits price/notes/date → Pending Approval; approve/reject w/ history; approval books an official job cost | **PARTIAL** *(refuted from IMPLEMENTED)* | Full data model + golden-path flow verified live (tests/e2e/production.spec.ts:93-214: tokenless submit→reject→resubmit→approve, job_costs row asserted via SQL) | `approveFactorySubmission`/`rejectFactorySubmission` (production/approve.ts:30-80) **never verify the caller-supplied jobId matches `request.jobId`** and use the raw parameter for the job_costs INSERT and status advance — a mismatched jobId (crafted client call) books the real factory cost on an unrelated job; no row lock on approve/reject/submit (concurrent double-approval can double-book the cost); cancelJob has no terminal guard so a cancelled job can still accrue an approved factory cost; expires_at/revoked_at are dead columns (see F5/RG9/RG10). |
| R1.30 | Job cost ledger (8 categories) as approvable transactions; profitability = revenue − approved costs, margin safe at zero revenue; gated behind VIEW_JOB_COSTS/VIEW_PROFITABILITY at the query level | IMPLEMENTED | Exactly 8 job_cost_category values; getJobProfitability (costs/queries.ts:113-133) documents itself as ungated and both its 2 real callers are correctly gated (jobs/[id]/page.tsx:457-467, api/reports/[report]/route.ts:150-156); marginPercent null-guarded at zero revenue | Gating is by caller discipline, not literally inside the query function — confirmed safe today (both real call sites gate correctly) but fragile to a future refactor. Note ProductionSection's factory-price leak (R1.10) bypasses this same protection for a different cost figure. |
| R1.31 | External contractors (name/phone/service-type/cost/job/item/notes), lightweight | PARTIAL | contractors table (contractors.ts:10-20); assign-dialog.tsx→assignToJob; add-cost-dialog.tsx→addJobCostAction (externalContractorId) | No create/edit UI or action exists at all — the only contractor is seed data; no item-level assignment; no "related work" view. |
| R1.32 | Repairs/Tikun per job (4 statuses); every unresolved repair visible on the dashboard | **SECURITY ISSUE** *(refuted from IMPLEMENTED)* | Data model + actions + dashboard attention row all correctly wired and involvement-scoped on the dashboard (getOpenRepairsCount uses restrictToUserId); repairs.spec.ts covers the full status lifecycle | **/repairs standalone list page has no involvement scoping at all** (repairs/page.tsx:50-66 gates only on CREATE_REPAIR; getRepairsList has no restrictToUserId param) — live-reproduced logged in as Basel (create_repair + view_assigned_jobs only, no view_all_jobs): GET /repairs returned all 6 company-wide repairs incl. 2 jobs whose own /jobs/[id] page correctly returns Forbidden ("هذه المهمة غير مسندة إليك") for the same session. |
| R1.33 | Short installation-completion form (items/qty/photo-flag/payment±amount±method/note); auto-saves technician/date/time; photo is WhatsApp-based yes/no flag | PARTIAL | complete-installation-dialog.tsx (items checklist, photo switch, payment switch+amount+method, note); complete-installation.ts:81-119 (race-safe transition, items→installed, conditional payment record) | Note is **silently discarded** whenever payment is not collected (only ever passed as the payment's own notes field); photo-taken flag is persisted nowhere except audit_logs JSON (no column, unqueryable); no completed_at/completed_by column on appointments; action has no ownership/assignee check at all (see §e, CRITICAL). |
| R1.34 | Vehicles (name/plate/fuel/responsible/value/active/notes); append-only responsibility history; default vehicle auto-fills Add Fuel; fast fuel entry; cost summary (fuel/maintenance/total/value) | PARTIAL | Full schema + actions (vehicles/actions.ts, fuel/actions.ts); default_vehicle_id auto-fills Add Fuel dialog; responsibility history has a partial-unique-index one-open-row guarantee | **No maintenance-cost feature exists at all** (no table/action/UI) — "total running cost" is fuel-only; monthly fuel window computed in UTC, not the company's Asia/Jerusalem day used elsewhere; zero automated tests for the module. |
| R1.35 | Dashboard: Today (measurements/installs/repairs) + 8-category Needs Attention; not overcrowded; salaries not dominant | **PARTIAL** *(refuted from IMPLEMENTED)* | All 9 categories present and permission-gated (dashboard/page.tsx:264-415); no salary/ledger figures rendered anywhere on the page | "Ready without install date" silently **excludes** jobs whose install appointment was booked then cancelled (query only filters by type, not by status≠cancelled, unlike the file's own today-appointments query 60 lines below it); "open repairs" scoping (involvementFilter) never checks `repairs.responsibleUserId`, so a technician set as a repair's sole responsible person never sees it on their own dashboard and gets Forbidden if they click the notification link. |
| R1.36 | Worker My Day: Call/Waze/Maps/Open-Job/Complete/Photo/Collect-Payment/Add-Note, permission-filtered | PARTIAL | All 8 actions exist and My Day is correctly assignee-scoped (appointments/queries.ts:202-282) | "Collect Payment" is **not permission-filtered** — neither the completion dialog nor completeInstallationAction checks COLLECT_PAYMENT, so any COMPLETE_INSTALLATION holder can record customer payments from My Day; Photo Taken/Collect Payment only reachable inside the installation-completion dialog, not standalone. |
| R1.37 | Reusable approval architecture (worker payments/technician reports/factory costs/custom installer amounts/fuel reimbursement/financial corrections); approve/reject w/ reason | PARTIAL | Shared approval_requests table + createApprovalRequest used by 5 flows (payments, technician ledger, factory, job costs, field expenses); unified /approvals queue | Custom installer amounts are auto-approved at creation, never routed through approval; fuel reimbursement/financial corrections have no action at all; rejection reason is optional for 3 of 5 flows and never displayed to the requester; queue visibility (APPROVE_REQUESTS) doesn't match the deciding permissions for 3 of the 5 flows. |
| R1.38 | Immutable audit log (who/what/when/entity/old/new) for sensitive changes | PARTIAL | audit_logs insert-only by convention (audit.ts:17-36, 84 call sites, 22 entity types); admin viewer (VIEW_AUDIT_LOG-gated) | old/new values are captured at only 18 of 84 call sites — quote price changes log only a version number (not old/new prices), and job status transitions inside advanceJobStatus are **never audited at all**; no DB-level immutability guard (trigger/REVOKE), convention only; UI renders raw English action slugs. |
| R1.39 | In-app notifications w/ read/unread for 9 listed event types | PARTIAL | Read/unread inbox works (notifications.ts, notifications-actions.ts); 26 distinct event types exist across 27 call sites | Only 2 of the 9 spec-named triggers exist (factory price submitted, worker payment for approval) — the other 7 are all time/condition-based (measurement-in-1h, installation-tomorrow, quote-awaiting-signature, ready-without-install, check-due, repair-open-N-days, customer-owes) and require a scheduler that does not exist (S5.1/RG11). |
| R1.40 | Fast global search by name/phone/job-number/address/quote-number w/ DB indexes | PARTIAL | search.ts:29-82 (customers by name/phone, jobs by number/customer name/phone), permission+involvement scoped; search.spec.ts (4 tests) | Address and quote-number are **not searched at all**; the only indexes present are plain btree, unusable by the leading-wildcard `ILIKE '%term%'` queries actually issued (no pg_trgm/GIN index). |
| R1.41 | Minimum-input/progressive-disclosure; sensitive fields (sale price/profit/factory cost/balances/others' salaries/job costs) protected front and back | SECURITY ISSUE | Correctly gated: profitability (VIEW_PROFITABILITY), commission, technician balances, company cash, other workers' salaries | Sale price rendered ungated on job page/list/customer page to any involved viewer including installers (jobs/[id]/page.tsx:275-282,416-420; jobs/page.tsx:136-138; customers/[id]/page.tsx:153-157); factory submitted price rendered ungated via ProductionSection (see R1.10); DB: 6 Basel-involved jobs carry sale_price_total, 3 factory_submissions on Basel-involved jobs. |
| R1.42 | Reports: Jobs/Customers/Technicians/Vehicles/Profitability; date/status/user filters; CSV export | PARTIAL | All 5 reports + CSV exist, permission-gated per report; escapeCsvField guards formula injection; **Live Probe 1**: /api/reports/jobs,customers,technicians,profitability → 403 for Basel as expected | No **per-user** filter on any report (spec-required); customers report has no filters at all; jobs report + customers report expose sale price / outstanding balance behind VIEW_ALL_JOBS alone, with no financial permission required (route.ts comment explicitly decides this is acceptable); **Live Probe 1**: ADD_FUEL alone (held by installers) exposes the vehicles report/CSV for every company vehicle, not just the viewer's own. |
| R1.43 | Data model prepared for future analytics (margins by work type, best closer) — model only | IMPLEMENTED | jobs.deal_closed_by_user_id; job_items.work_type_id + salePrice + expectedCost (already independently settable/readable); quote_items.work_type_id; compensation_rule_id/rate_used/closed_by_user_id; job_costs.category | None — exceeds the "model only" bar (some columns are already live-populated, not merely declared). |
| R1.44 | Working mobile navigation pattern equivalent to desktop sidebar | IMPLEMENTED | app-shell.tsx:113-167 (desktop `hidden md:flex` sidebar; mobile Radix Dialog drawer reusing the identical filtered NavLinks); live-verified at 375×812: hamburger opens a `role=dialog` with the same permission-filtered href set as desktop, closes on navigate | None found. |
| R1.45 | RTL setup app-wide: html dir/lang, icon mirroring, calendar localization | IMPLEMENTED | layout.tsx:25 `dir="rtl" lang="ar"`; 7 files apply `rtl:rotate-180` consistently (repo-wide grep found no un-mirrored directional icon); calendar-view.tsx:92-107 direction=rtl, Arabic buttonText | None found (note: calendar's *built-in* FullCalendar strings still leak English per R1.28 — a narrower, separate defect from icon/layout mirroring). |
| R1.46 | `npm run db:seed` idempotent; demo data for every implemented feature | PARTIAL | seed.ts:58-78 TRUNCATE…RESTART IDENTITY CASCADE lists 45/46 tables; cash_expense_reports cascade-truncates transitively via FK so rerun-safety holds | The field-expense-report feature (finance/expense-actions.ts, full UI) has **zero** seeded demo rows — invisible in the demo instance and untested by any suite. |
| R1.47 | Migrations 0000–0008 apply cleanly; DB reflects full history | IMPLEMENTED | Live DB `drizzle.__drizzle_migrations`: 9 rows matching meta/_journal.json 1:1 by id/hash/timestamp | Verified against an already-migrated DB (task scope forbids a from-empty-DB `db:migrate` run), not a from-scratch proof. |
| R1.48 | Security omnibus: hashing/sessions/authz/validation/CSRF/ORM-safety/XSS/token security/rate-limiting/no-committed-secrets/audit; client+server validation | PARTIAL | bcrypt 12 rounds, httpOnly/sameSite session cookies, revoke-on-password-reset; zod on every action; Drizzle parameterized queries; React+esc() XSS handling; 256-bit tokens; insert-only audit | **No rate limiting anywhere** — **Live Probe 2** confirmed 25 sequential failed logins against a real account (+972501111111) all returned 200 with no 429/lockout/backoff, latency flat at bcrypt-cost only, account still `status=active` after the flood (see S1.7, escalated to SECURITY ISSUE); factory links never expire; no manual revoke for either public-link type; password policy is length≥8 only. |
| R1.49 | Admin Settings (commission%/comp-penalty-bonus rules/statuses/vehicle-deduction/notification-thresholds/quote-validity/company-info/terms) | PARTIAL | 6 settings tabs, all gated MANAGE_SETTINGS, audited with old/new (settings-actions.ts:97-299); commission%, quote-validity, vehicle-deduction, check-due-soon, company-info are all genuinely consumed by runtime code | 3 of 4 notification-threshold settings and the default quote-terms setting are **dead controls** read by no runtime code — the form's own help text falsely tells the admin a reminder "will be sent"; company logo has no upload UI. |
| R1.50 | Every ~30-spec screen has an implemented route | PARTIAL | 28 real, non-stub page.tsx routes covering every named domain area | Cannot certify exact 1:1 parity against the ~30-screen spec list (that list lives in the missing checklist file) — route coverage is broad but not independently matched name-for-name. |
| R1.51 | Readable document numbering JOB-2026-0001 / Q-2026-0001 / production requests | PARTIAL | numbering.ts:14-39 (race-safe UPSERT sequence); live: job/2026=9, quote/2026=7 | Production requests are **never numbered** — no `request_number` column exists, no caller ever passes the 'production_request' scope; the PR- prefix constant is dead code. |
| R1.52 | Free PDF with full listed content; free e-signature stored w/ signed version; no over-claimed legal certification | **PARTIAL** *(refuted from IMPLEMENTED)* | playwright-core Chromium render, no paid service (live: 200, %PDF-1.4, 88KB); signature stored 1:1 with the version (unique constraint on quote_version_id, live-confirmed via psql); wording is plain "توقيع العميل", no certification claims | `notes` is declared on the PDF template's type but **never interpolated into the HTML** at all (and, per R1.20, never even collected) — one item of the required content list is entirely absent from every rendered PDF. |
| R1.53 | Notifications w/o paid services; scheduled processing via free infrastructure where needed | PARTIAL | In-app notifications are genuinely free/local (plain DB inserts, no provider) | **No scheduled processing exists anywhere** in the stack (no cron/worker/setInterval/pg_cron — confirmed via repo-wide grep and by docker-compose.yml's own self-documented gap) — this blocks 7 of 9 R1.39 notification triggers and all of R1.27's check reminders; explicitly a V1, non-deferrable requirement per the master prompt, not something the implementer was authorized to skip. |
| R1.54 | .env.example documents every environment variable read by the app | PARTIAL | 4 of 7 live-used vars documented (DATABASE_URL, CHROMIUM_EXECUTABLE_PATH, OLLAMA_BASE_URL, OLLAMA_MODEL) | OLLAMA_TIMEOUT_MS (ai/provider.ts:83) and DATABASE_POOL_MAX (db/client.ts:29) are read by the app but undocumented (both have safe defaults, so this is a documentation gap, not a functional one). See also D5 (BROKEN): `.env.example` itself is not even committed to git. |
| R1.55 | No real secrets/credentials committed | IMPLEMENTED | Broadened grep sweep (api-key/secret/password/token + AWS-key/private-key/sk-/ghp_ patterns): 0 real-secret hits; only documented demo/test passwords found; `.env` untracked, only `.env.docker.example` (placeholder 'change-me') is tracked | None. |
| R1.56 | Clean, descriptive commit history, no accidental artifacts | IMPLEMENTED | 76 commits, all descriptive; `git ls-files` has no .next/node_modules/storage/backups entries; 1294 objects/6.1MB, no oversized blobs | Untracked `.claude/worktrees/...` dev-tool leftover in the working tree (harmless, never committed). |
| R1.57 | Docker + backup/restore tooling internally consistent and safe | IMPLEMENTED | Dockerfile HEALTHCHECK+migrate-then-serve entrypoint; backup.sh/restore.sh both guard on env-file + running service, restore.sh requires typed "yes" | Not executed live in this audit (scope forbids container start/stop) — assessed by source read only. |
| R1.58 | Automated tests demonstrably pass | IMPLEMENTED | **This session's own live E2E run**: `npx playwright test` → 29/29 passed, 0 failed, 178s wall-clock (see §f/Sprint Status); refuter independently ran `npx vitest run` → 8 files/127 tests passed | A refuter found the *originally-cited* evidence file (`test-results/.last-run.json`) does not exist in the repo (Playwright's actual local artifact is gitignored `playwright-report/`) — a documentation-hygiene defect in the prior claim's citation, not a functional gap, and superseded here by this session's own fresh, reproducible run. |
| R2.1 | Individual cash-drawer tracking per worker; dedicated management audit view w/ date/time filters | PARTIAL | Real per-worker ledger + drawer section wired end-to-end (finance/technicians/[userId]/page.tsx:262-401); own-page access for any user, all-drawer access for VIEW_TECHNICIAN_BALANCES | "Dedicated management audit view" is only the same per-worker page reached one worker at a time — no cross-worker audit page; filters are date-only (no time-of-day, no creator, no job, no amount filter — see S6.6); adjustments have no creation path in the app (seed-only). |
| R2.2 | Business-date fields use the company's Asia/Jerusalem local day consistently | BROKEN | src/lib/company-day.ts is the one correct, tested implementation (company-day.test.ts, used by vehicles/actions.ts) | 6 other modules stamp "today" via raw `new Date().toISOString().slice(0,10)` (payments/record.ts:78; compensation/ledger.ts:127; production/approve.ts:77; costs/actions.ts:27-28; repairs/actions.ts:25-26; quotes/versions.ts:61) — company-day.ts's own comment explicitly warns against exactly this pattern, so this is a codebase-acknowledged, reproducible defect: any action in the ~2-3h window between Asia/Jerusalem midnight and UTC midnight is dated to the wrong calendar day. |
| R2.3 | Worker quick-submit measurement ("قياس جديد"): 1 button, file upload, optional notes/glass-type/phone/price w/ VAT toggle, lands on dashboard for review | IMPLEMENTED | Single full-width quick-action button (my-day/new-measurement-quick-action.tsx); phone required, rest optional; VAT toggle mandatory once a price is typed (submit disabled until chosen); measurements/actions.ts:107-348 mirrors every client constraint server-side; dashboard row for CREATE_PRICE holders | Glass-type list is not admin-editable (Q4); submission source is not a persisted field (Q6). |
| R2.4 | AI Hebrew quote from description+measurement via internal LLM; shareable link; mandatory Name/Phone/ID/Address before signing; auto-timestamp; signature/stamp pad; returning-customer autofill (address-only re-confirm) | PARTIAL | AI draft flow fully wired to Ollama (Hebrew SYSTEM_PROMPT, ai/quote-generator.ts:50-70); handoff into the real QuoteBuilderDialog; auto-timestamp server-side; name/customerId-scoped autofill (verified non-leaking across customers) | Server-side **only name+terms+signature are mandatory** — phone/ID/address are all `.optional()` in SignSchema (actions.ts:296-299), contradicting the "mandatory before signing" requirement; no stamp pad/upload exists at all; AI context excludes attachments, structured job data, and company default terms. |
| R2.5 | Post-signing automation: (a) abstracted installer job card w/ only address/contact/notes; (b) measurement attachment + glass specs auto-routed to factory | PARTIAL | (b, partial) Auto-created production request + factory link on signing (quotes/actions.ts:521-779), with item summary + glass-type label text | (a) is **entirely missing** — nothing is dispatched to installers on signing; what an installer eventually sees (via manual scheduling → My Day → "فتح المهمة") is the full management job page including sale price. (b) is text-only — the factory receives a sentence mentioning attachment *count*, never the actual image/PDF files or structured specs; the factory-page token has no way to fetch attachments at all (session-gated route only). |
| R2.6 | Installer UI/UX: minimum clicks, primary action buttons ("وصلت الموقع"/"تم التركيب"/"رفع ملاحظة"), no nested menus | IMPLEMENTED | Live-verified at 375×812 as Basel: single-column flat cards, one-tap arrived-button (race-safe), one-field note dialog, one-dialog completion flow; flat mobile drawer nav, no sub-menus anywhere in NAV_ITEMS | Completion button is labelled "إكمال التركيب" not literally "تم التركيب" (cosmetic, requirement text frames these as illustrative "such as" examples); no unit/e2e test exercises markAppointmentArrivedAction or addFieldNoteAction directly (only completion is e2e-covered). |

## (d) Master-prompt checklist — P, Q, A, I, F, G, V, C, S1–S10, T, RG, D

| ID | Requirement | Status | Evidence | Gap |
|---|---|---|---|---|
| P1 | Financial truth only from transactions (no editable total columns) | IMPLEMENTED | DB scan: no balance/paid/remaining/total column on any transactional table; jobs.sale_price_total written once, from convert.ts:69 only; every derived figure (paid/remaining/balance/cost/profit) computed via money.ts SUM at read time | None. |
| P2 | Decimal/numeric money everywhere; no float arithmetic on amounts | IMPLEMENTED | Every amount/price/cost/rate/wage column numeric(12,2)/(5,2); all persisted arithmetic routes through money.ts; commission_rate_percent validated server-side via parsePercent before touching Decimal | None material. |
| P3 | Server-side authorization on every read/mutation; no client-side-only gating | PARTIAL | All actions/routes gate server-side; involvementFilter applied to searches; sections omitted server-side, not CSS-hidden | /customers list ungated; query layer (jobs/queries.ts, quotes/queries.ts:239-243 listQuotes) has no built-in permission checks by design — every caller must remember to gate, and /quotes list does not scope by involvement. |
| P4 | Server never sends unauthorized sensitive values (sale price/factory cost/job cost/profit/margin/commission/balances/others' salaries) | SECURITY ISSUE | Correctly withheld: profit/margin, commission, technician balances, company balance, others' salaries | Sale price and factory cost are server-rendered into HTML for any involved viewer with no financial permission (jobs/[id]/page.tsx, jobs/page.tsx, customers/[id]/page.tsx, ProductionSection, /api/quotes/[quoteId]/pdf); DB confirms 6 Basel-involved priced jobs, 3 Basel-involved factory-priced jobs. |
| P5 | Signed quote version immutable; material item/price/term changes create a new version | **PARTIAL** *(refuted from IMPLEMENTED)* | quote_versions/quote_items rows are genuinely INSERT-only at the row level (confirmed: repo-wide grep finds no update/delete besides isSigned flip) | Row-level immutability is real, but the **displayed** item description on an already-signed quote is a live join to `work_types.labelAr`, not a frozen value — editing a work type's label (a routine, quote-unrelated MANAGE_SETTINGS action) silently changes what a signed quote's PDF/page shows, without a new version or new signature. |
| P6 | package.json contains only free/OSS dependencies | IMPLEMENTED | Full dependency list re-read: Next.js, Radix, Drizzle, pg, zod, decimal.js, playwright-core, recharts, bcryptjs, Tailwind, vitest — no proprietary package found | None. |
| Q1 | Technician can originate a new lead from the field without a pre-existing Job | IMPLEMENTED | measurements/actions.ts:263-272 inserts a new jobs row inside the same transaction, no jobId input on the form; gated CREATE_MEASUREMENT only; live DB permission check confirms Basel (7 keys, no VIEW_ALL_JOBS/CREATE_CUSTOMER) can drive it | None. |
| Q2 | Fields: optional name/phone/glass-type/attachment/notes/reference-price/VAT state | IMPLEMENTED | new-measurement-form.tsx:138-258 matches exactly; server mirrors every constraint (measurements/actions.ts:73-80,137-173) | None. |
| Q3 | Field price is reference only, never the commercial sale price | IMPLEMENTED | fieldQuotedPrice used only for display (page.tsx:349-360); jobs.sale_price_total is written only by quotes/convert.ts:66-81, never by the quick-submit path; live: JOB-2026-0009.sale_price_total = NULL | None. |
| Q4 | Glass type is its own admin-editable material-property list, distinct from work category | PARTIAL | Correct data-model separation (glass_types table distinct from work_types); 6 seeded rows | No admin CRUD exists for glass types at all (settings has no glass-types tab; lookups/actions.ts has no corresponding action) — seed-only list, despite a schema comment claiming otherwise. |
| Q5 | Submission becomes a real Job/lead in pending-review state, no disconnected inbox | IMPLEMENTED | field_submission_pending is a real job_statuses row (sort_order 3); the job proceeds through the normal jobs table and normal actions (cancel, advance) like any other job | None. |
| Q6 | Auto-filled: logged-in technician, timestamp, submission source | PARTIAL | Technician + timestamp correctly server-set (measurements/actions.ts:269-291) | Submission "source" is not a persisted, queryable column on jobs/measurements — it survives only inside an audit_logs JSON blob and is lost once the job leaves its initial status. |
| Q7 | Customer find/reuse by phone; safe create otherwise; no duplicates under concurrency | IMPLEMENTED | Partial unique index `customers_phone_active_unique_idx ... WHERE deleted_at IS NULL` (live-confirmed via psql `\d customers`), exactly matched by the `onConflictDoNothing` target/where clause | Concurrency safety is by sound DB-constraint reasoning (the unique index genuinely prevents duplicates); only sequential-submission coverage exists in the e2e suite, not a true concurrent-request test. |
| Q8 | Management notified on submission | IMPLEMENTED | notifyUsers fans out to every active CREATE_PRICE holder post-commit (measurements/actions.ts:333-342) | None. |
| A1 | AI drafts Hebrew wording from measurement+attachment+description+structured job data+company terms | PARTIAL | Description + latest measurement free-text + glass-type label genuinely reach the model (ai/quote-generator.ts) | Attachments (images/PDF) are never sent to the model; structured job data (title/existing items/customer) is not included; the company's configured default terms are never read by the prompt — the model invents terms from scratch instead of starting from company terms. |
| A2 | Admin reviews/edits via the structured builder; AI never commits prices | IMPLEMENTED | generateQuoteDraftAction never writes quotes/versions/items (confirmed by full-file read); only path to commit is saveQuoteDraft, itself CREATE_QUOTE-gated with independent zod/money validation; provenance flags (is_ai_generated/ai_prompt_notes) recorded on the saved version | None. |
| A3 | Quote creation works when AI is unavailable | IMPLEMENTED | Manual builder has zero code dependency on src/server/ai/*; every Ollama failure mode wrapped in AiProviderError and surfaced as a clean form error, never a page crash | None. |
| A4 | Default AI provider free/local; paid API optional, never mandatory | IMPLEMENTED | provider.ts is the sole AI call site, defaults to local Ollama, no API-key env var anywhere (repo-wide grep for openai/anthropic/api_key: 0 hits) | No configurable alternative provider exists (switching requires editing source) — "optional" only in the sense none is required. |
| A5 | Admin can open an incoming measurement and enter وصف الشغل from there | IMPLEMENTED | Dashboard → job page → measurement card → AI trigger's "وصف الشغل" textarea, feeding that job's latest-measurement context | Two navigation hops rather than a dedicated inbox; the entry point disappears once any quote already exists for the job. |
| I1 | Installers do NOT get the full management Job page by default | MISSING | My Day "فتح المهمة" links straight to the full /jobs/[id] page | The assigned-installer job page is the same management page minus a few gated cards; sale price, quote prices, and factory prices remain visible (see P4/R1.41); no abstracted installer-card route exists anywhere in the app. |
| I2 | After signing, only assigned installers get the installation job card | PARTIAL | Server-side involvement enforcement is real (unassigned VIEW_ASSIGNED_JOBS users correctly get Forbidden on jobs/[id], /jobs list, My Day) | No installer dispatch happens on signing at all (runPostSignAutomation never touches job_assignments/appointments); the only "card" is My Day, reachable only after a separate manual appointment is scheduled; the linked page is the full management page with prices. |
| I3 | If no installer assigned after signing → "Needs Installer Assignment" management attention item; assigning grants access | MISSING | Access-on-assign half works (involvementFilter includes job_assignments) | The attention item does not exist anywhere in the query layer, dashboard, or notifications (grep across dashboard/needs-attention.ts and notification types: 0 hits); DB shows 3 signed/in-production jobs with 0 assignments producing no attention item. |
| I4 | Installer card shows customer/phone/address/Call/Waze/Maps/appt-time/items/notes/required-production-info + actions, payment only if authorized | PARTIAL | My Day card has customer name, Call/Waze/Maps, appointment time, arrived/note/complete actions, correctly assignee-scoped | Card omits appointment notes and job notes (not even queried), production/measurement info, and item list (only inside the completion dialog); address shown only as link targets, not visible text; payment-collection switch is present with **no COLLECT_PAYMENT check at all** — any COMPLETE_INSTALLATION holder can record payments. |
| I5 | Installer card/payload excludes sale price/profit/margin/factory cost/internal costs/commission/others' compensation/company cash/customer national ID | SECURITY ISSUE | My Day card payload itself is clean (MyDayAppointment carries no money fields); profit/margin/commission/company-cash/others'-compensation correctly excluded everywhere | The card links to the job page, which delivers sale price and factory submitted price (per R1.10/R1.41) to the assigned installer; customer national ID is reachable by any VIEW_CUSTOMERS holder via /customers/[id] (per R1.15, now escalated by live probe). |
| I6 | Sale price requires a separate commercial/financial permission | MISSING | No permission key exists for sale-price visibility anywhere in permission-keys.ts; none of the 4 surfaces that render it (job page, jobs list, customer page, quote PDF) check any financial permission | None to gap-fill — this is a straightforward absence, confirmed by grep. |
| F1 | Factory receives the actual measurement attachment(s), not a text mention | MISSING | buildMeasurementMentionLine (quotes/actions.ts:495-512) returns only a sentence: glass-type + "يوجد N مرفق"; the doc comment explicitly states attachments are deliberately never linked from the factory page | No mechanism delivers file bytes or a token-scoped link to them; attachments remain reachable only through the session-gated /api/attachments route, which a factory token cannot use. |
| F2 | Factory page exposes attachments/specs/dimensions/quantities/notes/relevant items | PARTIAL | Quantities, item names, production notes (free text), glass-type name (auto path) are present | Attachments absent (F1); no structured dimension/spec fields (free-text only); auto-generated details interpolate the raw unit key instead of the localized label used by the manual path. |
| F3 | Factory page excludes sale price/payments/profit/commission/compensation/customer-ID/unrelated notes | IMPLEMENTED | getProductionRequestByPublicToken selects only link/request/job(number,title)/submissions — no join to customers/quotes/payments/costs/commissions/ledger anywhere; live-fetched real token rendered zero matching terms for any of those fields | Residual risk is human (request.details is free text staff can type anything into), not a code gap. |
| F4 | Factory files protected by the production token | MISSING | No factory-accessible files exist at all (no route accepts a factory token for file retrieval) | Because F1 is unbuilt there is nothing for the token to protect — a missing feature, not a leak (the session-gated attachment route correctly 401s a tokenless factory contact). |
| F5 | Production token supports revocation and expiration; invalid/expired/revoked handled cleanly | PARTIAL | Invalid-token handling is clean (live-verified: 200 w/ Arabic "invalid link" notice); read-side revocation check exists and is honored | `expiresAt` is never set by createProductionRequest and never checked anywhere (live DB: 0/5 factory links have expires_at); `revokedAt` is only ever read, never written by any action — a leaked factory link is valid forever and cannot be revoked. |
| G1 | Mandatory before signing: name/phone/ID-or-company-ID/address/terms/signature, server-enforced | PARTIAL | SignSchema requires only name, terms-acceptance, and signature (actions.ts:295-307) | Phone, national ID/company ID, and installation address are all `.optional()` server-side — the client form doesn't even mark them `required` (noValidate); live DB: 4 of 6 signatures have null national_id. |
| G2 | Auto-saved at signing: date/time/version/customer-data/totals/terms/signature (immutable snapshot) | IMPLEMENTED | Full transaction writes quote_signatures with server-set signedAt, signing-time customer fields, lat/lng, agreedToTerms, signature image, IP, plus quotes.signedVersionId + version.isSigned=true; unique constraint on quote_version_id live-confirmed | Snapshot excludes company info and mutable work-type labels the PDF actually prints (see P5/G7); no stored PDF artifact. |
| G3 | Customer never enters date/time manually | IMPLEMENTED | sign-form.tsx has no date/time input anywhere; signedAt = `new Date()` set server-side | None. |
| G4 | Company stamp attachment/pad for company customers | MISSING | grep for stamp/חותמת/ختم (excluding "timestamp") across src and docs: 0 hits; SignSchema has exactly one signatureImage field, no stamp column on quote_signatures | Not built at all — signing offers only a hand-drawn signature, for individuals and companies alike. |
| G5 | Returning-customer legal info auto-filled securely, no cross-customer leakage | IMPLEMENTED | localStorage cache key namespaced by server-resolved customerId (never client-supplied); address deliberately excluded from the cache | Legal ID persists in plaintext localStorage on whatever device signs, with no expiry/clear control — inherent to the localStorage requirement itself, not a code defect. |
| G6 | Installation address editable/confirmable per new Job | PARTIAL | Customer can edit/confirm an address at signing (Textarea pre-filled from the customer master, never cached) | The confirmation is optional and is written only to quote_signatures — it never becomes the job's installation address and is never shown to staff anywhere, so confirming it has no downstream effect. |
| G7 | Signed PDF renders exactly the signed version | PARTIAL | Correct while the signed version is still the current one (live: seeded signed quote renders fine, 88KB) | Both PDF routes render `quotes.currentVersionId`, not `signedVersionId` — once staff save a v2, the v1 signed PDF becomes unreachable (public route 404s the revoked link; internal route silently renders the new unsigned draft); the template also pulls live customer/company/work-type data rather than a signing-time snapshot even when signed==current. |
| V1 | Requester cannot approve their own request (requester_id ≠ approver_id) | MISSING | No `decide`/`confirm` action anywhere compares requester to decider (decide.ts:69-97; costs/actions.ts:196-292; compensation/actions.ts:599-708; expense-actions.ts:163-288; production/approve.ts:30-129; transfer-actions.ts:154-232 — all checked, none has the comparison) — confirmed independently by both the auth-security and finance domains | Several paths auto-approve the requester's own entry at creation (payments/record.ts, costs/actions.ts, compensation/actions.ts); DB: 4 customer_payments and 5 job_costs are self-approved (created_by = approved_by). |
| V2 | Super-admin override only if explicitly authorized, logged, visible in audit history; never silent self-approval | MISSING | No override/self-approval permission key exists in permission-keys.ts; auto-approved writes create no approval_requests row at all (bypassing the queue entirely) | Audit rows for auto-approved writes carry only a generic `autoApproved:true` flag, never an "override"/"self-approval" marker; nothing in the admin audit-log UI surfaces requester==decider joins. |
| C1 | Commission basis = final effective signed version (re-signed v2 supersedes v1) | IMPLEMENTED | computeCommission always reads current jobs.salePriceTotal; applySignedQuoteToJob unconditionally overwrites it on every (re-)conversion, manual or automatic; live DB: JOB-2026-0002.sale_price_total = 1200.00 matches v2 (signed), not v1's 900.00 | Nothing re-estimates or flags an already-finalized commission when a v2 is signed afterward — it silently keeps v1's figures. |
| C2 | Estimated commission updates while costs incomplete; final = final gross profit × configured % | PARTIAL | Both estimate and finalize recompute correctly from current approved costs at the moment they're invoked; finalize is race-safe (conditional UPDATE WHERE status != 'finalized') | Estimate only refreshes when a manager manually clicks the button — it does **not** auto-update as costs are approved or a quote is re-signed, so the displayed figure can be stale. |
| C3 | Historical estimates preserved, not silently overwritten/deleted | PARTIAL | Final figures are correctly preserved once finalized (estimated_* kept alongside final_*) | Estimates themselves are overwritten in place on a single unique-per-job row with no version history — and this is possible even *after* finalization (estimateCommission has no status guard); only the audit log retains prior values, and it is not surfaced on the job page. |
| S1.1 | /customers requires the correct permission | SECURITY ISSUE | customers/page.tsx:33-40 has no VIEW_CUSTOMERS check (only gates the "add" button); listCustomers() has no viewer scoping; nav-hiding is the only gate | Any authenticated user can retrieve the full customer directory (name/phone/address/job-count) via /customers, searchable by name/phone. |
| S1.2 | Every Job read/update/delete action validates authorization | SECURITY ISSUE | Reads correctly gated (VIEW_ALL_JOBS or involvement) | Mutations check only a permission key, never job visibility — createMeasurement lets a VIEW_ASSIGNED_JOBS+CREATE_MEASUREMENT holder self-grant full read access (incl. sale/factory prices) to **any** job by setting themselves as measuredBy; scheduleAppointmentAction/createRepairAction/addPaymentAction/cancelJob/closeJobAction/sendQuoteAction all act on any caller-supplied job id with no visibility check. |
| S1.3 | Child-entity actions verify the entity belongs to the referenced Job; never trust client Job IDs | SECURITY ISSUE | Some actions correctly derive job from the row (decideJobCostAction, decideCustomerPaymentAction, updateRepairStatusAction) | 9+ actions trust the client jobId or act on child ids with no ownership check — deleteJobItem/removeAssignment delete by row id only; cancelAppointmentAction has no jobId match or status precondition; completeInstallationAction marks arbitrary jobItemIds installed with no jobId filter; approveFactorySubmission/rejectFactorySubmission use the caller's jobId instead of request.jobId, corrupting financial data cross-job; sendQuoteAction/convertQuoteToJob never verify the quote belongs to the job. Audit rows record the attacker-supplied jobId in every case, poisoning the trail. |
| S1.4 | Explicit permissions exist and are enforced for: sale price/job costs/profitability/technician balances/company balances | PARTIAL | Profitability, technician balances, company balances are correctly gated | Sale price has **no permission at all**; job-cost gating (VIEW_JOB_COSTS) is bypassed by ProductionSection's ungated factory-price rendering (R1.10). |
| S1.5 | Installers do not automatically see sale price | SECURITY ISSUE | — | An assigned installer automatically sees sale price on both the job page and jobs list; live DB confirms Basel (installer persona) is involved in 6 sale-priced jobs. |
| S1.6 | Unauthorized approval and normal self-approval are both prevented | PARTIAL | Unauthorized approval is correctly blocked everywhere (race-safe conditional UPDATEs gated by the right permission, proven by permissions.spec.ts:140-197) | Self-approval (requester==approver) is **never** checked anywhere (see V1); DB shows 4 self-approved payments + 5 self-approved job costs live. |
| S1.7 | Rate limiting on quote links/factory links/login/signature submission | **SECURITY ISSUE** *(escalated by live probe)* | grep for rate-limit/throttle/lockout code: 0 hits anywhere in the stack | **Live Probe 2**: 25 sequential wrong-password POSTs to /login against a real, currently-active account (+972501111111) all returned HTTP 200 with the identical generic error, no 429, no increasing latency, account still `status=active` afterward — unlimited, unthrottled credential-guessing against a real account is live-demonstrated, not merely a missing feature. Same probe found 25 rapid requests to a valid public quote link and its PDF endpoint (which drives a real headless-Chromium render per call) also entirely unthrottled. |
| S1.8 | Public tokens: random/secure, expiration, revocation, invalid-token handling | PARTIAL | Tokens are genuine 256-bit random (tokens.ts:10-12); invalid-token handling is clean for both link types (live-verified: malformed/random/SQLi/traversal/oversized/null-byte tokens all return the generic "invalid link" page, no crash, no leak — **Live Probe 2** ran 10+ adversarial token variants, all handled gracefully); quote-link expiry works and is enforced at signing | Factory links never expire (expires_at always NULL live) and neither link type can be manually revoked/regenerated by staff (no action exists for either). |
| S1.9 | Automated tests prove unauthorized users cannot RETRIEVE sensitive data (server responses, not hidden UI) | PARTIAL | Real response-level coverage exists for ledger access, uninvolved-job access, attachment 403, revoked-PDF 404 | No test anywhere asserts an installer cannot retrieve sale price or factory price — and per R1.41/S1.5 they demonstrably can; API-route 403s for /api/reports/* exist only in a standalone verify script outside the Vitest/Playwright suites. |
| S2.1 | Mobile UX: minimum taps from My Day to submitted | IMPLEMENTED | One tap from My Day to a dedicated full-screen form; only phone+files required; autoFocus, inputMode='tel', VAT section appears only once a price is typed | None material. |
| S2.2 | Server validation + automated tests exist for the quick-submit flow | **PARTIAL** *(refuted from IMPLEMENTED)* | Server validation is solid and fully wired (actions.ts:116-173); file-validation (16 tests) and phone/dedup logic (17 tests) are genuinely unit-tested | The project's own design spec explicitly requires a 3rd Vitest area — reference-price + VAT-label display formatting — and it has **zero** coverage anywhere; the one e2e assertion touching VAT only clicks a selector, never asserts the rendered label/price text, so a swapped VAT label would pass every existing test undetected. |
| S4.1 | Correct version marked signed; job status never moves backwards | IMPLEMENTED | isForwardStatusMove/advanceJobStatus is a strict sort_order gate, unit-tested 4/4; live DB confirms strictly increasing sort_order through the quote-related sequence; row-locking closes the convert-time race | A narrow non-transactional read window in signQuotePublicly could theoretically attach a signature to a version other than the one displayed (mitigated by link auto-revocation on new version); sendQuoteAction has no status/terminal check and can flip a signed quote's status back to 'sent'. |
| S4.2 | Auto-conversion transfers customer/phone/address/location/measurement/items/notes/attachments/signed-reference (no re-entry) | PARTIAL | Items, total price, and the signed-version reference are transferred automatically; customer/measurements/notes need no re-entry since they belong to the pre-existing job | Signing-time address/lat/lng/phone/ID (captured in quote_signatures) are **never** copied to jobs.address/lat/lng or the customer record — dead data; attachments are not routed to the factory (text mention only, see F1); re-conversion wipes and re-inserts job_items, losing item status/attachment links. |
| S4.3 | Factory route auto-created with attachments + specs | PARTIAL | Auto-creation genuinely runs on signing, guarded by a job row lock, terminal-job check, and existing-request check; live DB shows a real auto-issued request with matching timestamp/notification pair | Content is text-only (item summary + glass-type label + attachment *count*) — no attachment files or structured specs are ever linked (same root cause as F1). |
| S4.4 | Installer route: assigned installers get restricted cards; none assigned → attention item | PARTIAL | Assignee-scoped My Day card exists (after manual scheduling); unassigned installers correctly Forbidden on the job page | No signing-triggered installer routing exists at all; the restricted card, once reached, leads to the full job page with prices; the attention item does not exist (see I3). |
| S5.1 | A scheduler exists on free/self-hosted infrastructure (V1, not deferrable) | MISSING | docker-compose.yml defines only postgres+app; Dockerfile entrypoint is migrate-then-serve only; repo-wide grep for cron/scheduler/setInterval/worker/pg_cron: 0 hits; gap is self-documented in docker-compose.yml/ARCHITECTURE.md/DEPLOYMENT.md | Nothing runs on a schedule anywhere in the stack — this is explicitly named a non-deferrable V1 requirement in the master prompt, not something the implementer was authorized to skip. |
| S5.2 | Scheduled notifications (measurement-1h/install-tomorrow/quote-waiting/ready-no-install/check-due/check-overdue/repair-open-N-days/customer-balance) | MISSING | Exhaustive list of all 26 live notification types contains none of these 8 | Zero of the 8 exist; 4 of the underlying conditions are visible only if a user happens to open the dashboard (no push/notification ever fires for them). |
| S5.3 | Factory submission/approval notifications work | IMPLEMENTED | Full round trip traced and verified: submit→notify all APPROVE_FACTORY_PRICE holders; approve/reject→notify requester (skipping self); href resolves correctly; API route requires session | submitFactoryPriceAction's status pre-check has no row lock — two near-simultaneous public submissions could both pass and both fire notifications (produces duplicate notifications, not missing ones). |
| S5.4 | Notification has recipient/type/entity/created-time/read-unread/optional action URL | PARTIAL | All required columns exist and are populated (system.ts:55-77); action URL correctly derived for job-linked notifications | The two cash_transfer notification types resolve to `/finance/cash`, a route that **does not exist** (404 on click) — confirmed by `find` across src/app/(app)/finance; the same stale path appears in 3 separate revalidatePath calls. |
| S5.5 | Idempotent scheduled processing; no duplicate spam | MISSING | — | No scheduled processing exists at all (S5.1), so there is nothing to be idempotent; no dedupe key or last-notified marker exists on any table that a future scheduler could rely on. |
| S5.6 | Tests verify scheduled conditions | MISSING | — | Zero unit or e2e tests reference notifications/reminders/thresholds/a scheduler at all; only untracked, non-CI verify scripts touch event (not scheduled) notifications. |
| S6.1 | Customer page shows profile/quotes/jobs/payments/checks/balance/repairs/activity | PARTIAL | Profile + jobs list present (2 of 8 sections) | 6 of 8 required sections (quotes/payments/checks/balance/repairs/activity) are entirely absent — no query exists for any of them. |
| S6.2 | Job summary shows Operational Status and Payment Status independently | PARTIAL | Operational status badge shown in the header | No payment-status element exists anywhere in the header/summary — paid/remaining figures appear only deep inside the (permission-gated) Payments card, never as an independent status field. |
| S6.3 | Payment Status label (Not/Partially/Fully Paid) computed automatically | MISSING | grep across src for the Arabic labels or any "paymentStatus" identifier: 0 hits | The inputs (sale price, approved payments, remaining) all exist; no classification function or badge was ever built on top of them. |
| S6.4 | Sale price / paid / remaining shown only when authorized | SECURITY ISSUE | Paid/remaining ARE correctly gated (canViewPayments) | Sale price is delivered with **no permission gate at all** on the job page, jobs list, and customer page (Server Components, so values reach the unauthorized viewer's own HTML, not merely hidden) — includes cross-job exposure via the customer page, which isn't even involvement-scoped. |
| S6.5 | Each worker has an individual company-cash ledger view (collected/handovers/expenses/adjustments/balance) | IMPLEMENTED | Real, correctly-authorized per-worker section (own page or VIEW_TECHNICIAN_BALANCES); 5-category source labeling matches the requirement | "Adjustment" rows have no creation path anywhere in the app (seed-only), so that category can never appear for live data — a completeness gap, not an authorization one. |
| S6.6 | Management audit view filters: worker/date-range/type/Job/amount/creator | PARTIAL | Worker (per-page) and date-range filters exist | Transaction-type filter is direction (in/out) only, not source type; Job, amount, and creator filters do not exist at all — creator (createdByUserId) is fetched but never even displayed in the table. |
| S6.7 | Cash balance never manually editable | IMPLEMENTED | No balance column exists anywhere; every cash_transactions write is tied to an approved/confirmed source row with no update/delete path anywhere in the codebase | Side effect: because no adjustment action exists, a genuine cash-count discrepancy cannot be corrected except by a direct DB edit; drawers can also go negative (no balance check on debit). |
| S7.1 | Job-level AND job-item-level installer assignment (UI + action) | PARTIAL | Job-level assignment fully works (assignToJob, multi-assignee, removeAssignment) | Item-level assignment (jobAssignments.jobItemId) is schema-only — no action parameter, no UI selector, no display anywhere; live DB: 0 item-level rows. |
| S7.2 | Contractors: create/edit/assign-to-job/assign-to-item/record-costs/view-related-work | PARTIAL | Assign-to-job and record-costs both work | Create/edit, assign-to-item, and a related-work view are all entirely missing — only the seeded contractor exists in the system. |
| S7.3 | Ledger adjustments: overtime/overtime-amount/fuel-reimbursement/personal-vehicle/bonus/penalty/other, w/ approval where required | PARTIAL | 3 of 7 categories wired (personal vehicle, bonus, penalty) | Overtime is not even in the ledger_entry_type enum; fuel_reimbursement and other_adjustment are enum-only with zero writer; every manager-recorded adjustment is auto-approved immediately, with no approval routing for any of them (only the technician's own self-report goes through the queue). |
| S7.4 | Production request numbering PR-2026-0001 | MISSING | numbering.ts declares the 'production_request' scope but no caller ever passes it; production_requests has no number column at all | Not implemented beyond a dead prefix constant. |
| S7.5 | Public link management actions: revoke/regenerate/expiration | SCHEMA ONLY | expires_at/revoked_at columns exist on factory_public_links | None of the 3 operations has a server action or UI anywhere — the data model anticipates all three, application code implements none. |
| S7.6 | Vehicle page: monthly fuel/maintenance/other-costs/total-operating-cost/estimated-value | PARTIAL | Monthly fuel, fuel-only "total running cost", and estimated value are all shown (and the page itself honestly states maintenance/other costs don't exist) | Maintenance and other costs cannot be recorded or displayed at all — no schema, action, or UI for either. |
| S8.1 | No screen ships a placeholder or dead primary action button | IMPLEMENTED | Repo-wide grep (dead onClick, href="#", TODO/FIXME/"not implemented", disabled={true}, bare console.log/alert): 0 hits anywhere | Verification is static grep + spot-checks, not a manual click-through of all 28 routes. |
| S8.2 | Row-level/secondary action controls (remove/edit/status-change) wired to real handlers | IMPLEMENTED | ConfirmRemoveButton, RepairStatusControl, CheckStatusControl all dispatch real imported server actions with toast success/error feedback | None found. |
| S8.3 | Installer primary actions: وصلت الموقع/تم التركيب/رفع ملاحظة/اتصال/Waze/Maps/جمع دفعة | PARTIAL | 5 of 7 exist as specified (arrived, note, call, Waze, Maps); completion exists under the label "إكمال التركيب" | "جمع دفعة" does not exist as a standalone primary action — payment collection is buried inside the installation-completion dialog only, unavailable before/outside completion, and not gated by COLLECT_PAYMENT. |
| S8.4 | Mobile-first, no nested menus, large targets, few fields | IMPLEMENTED | viewport meta + `lang=ar dir=rtl`; buttons default h-11 (44px) matching the coarse-pointer 2.75rem minimum; My Day is a genuine single-column card list; add-fuel-dialog has exactly 3 required fields | Adjacent observation: `maximumScale:1` disables pinch-to-zoom (an accessibility tension, not covered by this item's own wording). |
| S9.1 | Screens show a loading state while server data is fetched | MISSING | `find src/app -name loading.tsx`: 0 results app-wide, including the (app) group and jobs/[id] segment; Job Details awaits 17 queries in one Promise.all with no Suspense fallback anywhere upstream | No route-level loading UI exists anywhere in the app; only 3 unrelated in-component spinners exist (comboboxes, an AI-draft button). |
| S9.2 | Screens show a localized empty state | IMPLEMENTED | `EmptyState` component used in 35 files, all with Arabic copy; Job Details alone uses it 3× (measurements/items/assignments) | Verified widespread via grep + direct reading, not an exhaustive click-through of every list on every screen. |
| S9.3 | Screens show a localized error state, incl. a proper 404 for a nonexistent entity, and an exception boundary | BROKEN | 5 dynamic detail pages call `notFound()` with no custom `not-found.tsx` to catch it anywhere in the tree | **Live-reproduced**: GET /jobs/00000000-0000-0000-0000-000000000000 renders Next.js's default, unstyled **English** "This page could not be found." in an otherwise 100%-Arabic-RTL app — a stale or mistyped link (routine in a field-service app shared via WhatsApp/SMS) drops the user out of the app entirely with no way back; no `error.tsx`/`global-error.tsx` exists at any level either, so a thrown exception falls back to the same unlocalized default. |
| S10.1 | A real Vitest unit-test suite exists covering pure business logic | IMPLEMENTED | vitest.config.ts scoped to `src/**/*.test.ts`, node env; live run (this audit + independently by a refuter): 8 files / 127 tests, all passed; spot-read money.test.ts shows substantive edge-case coverage, not smoke tests | None. |
| S10.2 | Enumerate which of the checklist's functional areas have real test coverage | PARTIAL | 8 areas have dedicated e2e specs (auth/permissions, payments/cash, factory production, quote versioning+signing, repairs, search, field-measurement, the full Ahmad scenario); 8 more have unit-only coverage | Several server modules have **zero** test coverage of any kind: `src/server/ai`, `src/server/audit`, `src/server/checks`, `src/server/dashboard`, `src/server/fuel`, `src/server/lookups`, `src/server/pdf`, `src/server/users`, `src/server/vehicles`, and specifically `finance/expense-actions.ts` (also zero seed data, see R1.46). |
| S10.3 | The Ahmad end-to-end scenario test exists and runs the full worked example through real UI | IMPLEMENTED | tests/e2e/ahmad-scenario.spec.ts (528 lines) creates a brand-new lead and drives every step through real UI clicks across 4 login contexts + 2 public-link contexts, not a re-read of seeded data | None. |
| S10.4 | The Ahmad scenario verifies computed totals/outcomes against actual DB rows, not just page text | IMPLEMENTED | Direct SQL assertions for measured_by/pricing_responsible independence, factory_glass cost amount, 2 installer_labor rows, SUM of approved costs, commission fields cross-checked against a live-read setting, and net-zero cash-handover deltas | None. |
| T1 | Super Administrator: full access | IMPLEMENTED | Live DB: 32/32 grants for عمرو, matching `permissions` table count exactly; session re-reads user_permissions every request (no stale-role caching) | None. |
| T2 | Commercial/Manager persona: measure/price/quote/close/approve/production/profitability | IMPLEMENTED | Live DB: محمد has exactly the 25 MOHAMMAD_KEYS seeded, independently verified live-usable in application code, not just present in seed data | None for the persona definition itself. |
| T3 | Sales/measurement employee loses pricing immediately when the permission is revoked | **PARTIAL** *(refuted from IMPLEMENTED)* | The underlying mechanism (per-request permission reload, no cross-request cache) is sound and independently confirmed | The specific cited tests (permissions.spec.ts:60-74,145-197) revoke `create_measurement`/`create_repair`/`approve_requests` — **none of them ever revokes CREATE_PRICE or EDIT_PRICE**; a repo-wide grep confirms zero test anywhere touches pricing-permission revocation, so the "loses pricing immediately" claim is plausible by analogy but not directly proven. |
| T4 | Installer/Technician: assigned jobs/measurements/notes/completion/allowed-payments/fuel; NOT profitability/costs/finance/unrestricted sale prices | SECURITY ISSUE | Persona's permission set is exactly correct (7 keys, no financial permissions); profitability/costs/company-cash all correctly withheld | The pages an installer *can* open reveal unrestricted sale prices and factory costs (same root cause as R1.41/P4/S1.5) — as a behavioral requirement, not just a permission-set requirement, this fails. |
| T5 | No name-based behavior anywhere | IMPLEMENTED | Same mechanism as R1.5, independently re-verified; all ownership checks confirmed id-based (`user.id`, `ownerUserId`, `fromUserId`) | None. |
| RG1 | Regression guard: stored-XSS-via-SVG-upload stays fixed | IMPLEMENTED | `src/server/storage/attachments.ts` `ALLOWED_IMAGE_MIME_TYPES` explicitly excludes `image/svg+xml` (with an in-code comment against re-adding it); `isAllowedAttachmentMimeType` is strict set-membership, not a `startsWith('image/')` prefix match; `tests/e2e/new-measurement.spec.ts:443-497` tampers the multipart request at network level (bypassing any client check) and asserts server-side rejection | None. |
| RG2 | Regression guard: global-search job-visibility leak stays fixed | IMPLEMENTED | `src/server/search.ts:37-40,66-76` — `restrictToUserId` computed correctly, `involvementFilter` applied to the jobs query's WHERE clause; `tests/e2e/search.spec.ts:101-146` verifies both "not visible before involvement" and "visible after `job_assignments` insert" via direct SQL + UI | None. |
| RG3 | Regression guard: `convertQuoteToJob` must set `jobs.dealClosedByUserId` on every path | IMPLEMENTED | Manual path: `quotes/actions.ts:281` (`dealClosedByUserId: user!.id`); auto path: `quotes/actions.ts:541-598` (resolved from `quoteCreatedByUserId ?? pricingResponsibleUserId`, with an explicit manual-fallback notification if neither resolves) | None. |
| RG4 | Regression guard: `cancelJob` must append the cancellation reason to `jobs.notes`, never overwrite prior field notes | IMPLEMENTED | `jobs/actions.ts:449-459` (doc comment on the prior bug) + `:479-491` genuine SQL-level `concat_ws(E'\n\n', jobs.notes, cancelEntry)` append, conditional on a reason being supplied | No dedicated e2e/unit test exercises `cancelJob`'s note-append behavior specifically (grep of tests found none) — fix present in source but not regression-tested. |
| RG5 | Regression guard: measurement-attachment retrieval must be authorization-gated | IMPLEMENTED | `src/app/api/attachments/[attachmentId]/route.ts:21-69` — requires auth + baseline `VIEW_ALL_JOBS`/`VIEW_ASSIGNED_JOBS`, then re-derives job involvement via `getJobDetail` and 403s otherwise, matching the job-detail page's own gate | None. |
| RG6 | Regression guard: auto-convert/auto-send-to-factory race + terminal-job guard stays fixed | IMPLEMENTED | `quotes/actions.ts:555-598` — `runPostSignAutomation` re-checks the job's terminal/already-converted state **inside** a `db.transaction` under `lockJobForWrite`, mirroring the manual path's own lock; code comments state this guard was previously entirely missing from the automatic path | None. |
| RG7 | Release gate: public quote links | IMPLEMENTED | Token issuance/revocation-on-new-version/expiry-check all verified live against the running dev server (bogus token → 200 w/ notice, PDF → 404) | No manual revoke/regenerate UI (see S7.5-equivalent for quotes); no rate limiting (see S1.7). |
| RG8 | Release gate: factory links work | IMPLEMENTED | Full issue→open→submit→decide loop verified live and in tests/e2e/production.spec.ts using a genuinely cookie-less browser context | The approve/reject IDOR (caller-supplied jobId, see S1.3/R1.29) and dead expiry/revocation columns are real gaps, but belong to the broader "secure link"/"official cost" wording (R1.29), not to this narrower "links work" gate, which does hold. |
| RG9 | Release gate: link revocation | PARTIAL | Read-side enforcement is real for both link types (revoked links correctly rejected) | For factory links, revocation has **no write path anywhere** — a leaked link can only be revoked by hand-editing the database; neither link type has a manual-revoke UI. |
| RG10 | Release gate: link expiration | SCHEMA ONLY | `expiresAt` column exists on factory_public_links | Never set by createProductionRequest, never checked anywhere; quote links (the sibling feature) do both correctly, making the omission clearly an oversight rather than a design choice. |
| RG11 | Release gate: scheduler runs | MISSING | No scheduler entrypoint/service/job exists to start (docker-compose.yml, Dockerfile entrypoint, package.json scripts all checked) | Gate cannot be exercised at all — nothing runs on a schedule. |
| RG12 | Release gate: Arabic mobile UI | PARTIAL | Shell + technician screens are genuinely Arabic/RTL and mobile-capable (layout.tsx, app-shell.tsx) | Calendar leaks English FullCalendar built-in strings (no locale import); job detail page (reachable from My Day) has no mobile layout pattern; no automated test exercises a mobile viewport or RTL rendering at all. |
| RG13 | Release gate: installer cannot access financial info | SECURITY ISSUE | Profit/margin/ledgers/company-cash correctly blocked (live-verified via /api/reports/profitability → 403 for Basel) | Sale price and factory cost — both named financial values — are retrievable by installers (same root cause chain: R1.10/R1.41/P4/S1.5/T4); release gate not met. |
| RG14 | Release gate: permissions enforced server-side | PARTIAL | Universal permission checks confirmed across all actions/routes/pages; live unauthenticated probes return 401/307 as expected | /customers list ungated; job mutations not scoped to visible jobs; child-entity IDOR (S1.3); no sale-price permission exists at all (I6). |
| RG15 | Regression guard: cross-customer PII leak in public signing autofill stays fixed | PARTIAL | Fix genuinely present in source: `customerId` is scoped to the token-resolved quote's own customer (public/q/[token]/page.tsx:203), not client-supplied | No dedicated regression test (unit or e2e) exercises this specific scenario — a repo-wide grep for "autofill"/"returning" across tests/e2e finds nothing, so a future regression would not be caught automatically. |
| D1 | Dockerfile builds a working, minimal production image | IMPLEMENTED | Three-stage build, standalone-only copy into runner, isolated migrate-only drizzle-kit install, non-root user, HEALTHCHECK against /login | Not built/run live in this audit (scope forbids it); assessed by source read (incl. confirming the build-stage's dummy DATABASE_URL is safe since pg.Pool never connects eagerly). |
| D2 | docker-compose.yml defines a coherent self-hosted stack | IMPLEMENTED | postgres (healthcheck-gated, no host port) + app (depends_on healthy), DATABASE_URL correctly assembled, named volumes for DB + attachments | File self-documents the one known gap (no scheduler service) rather than misrepresenting the stack. |
| D3 | Backup script safely dumps the Compose Postgres database | IMPLEMENTED | scripts/backup.sh guards on env-file + running service before any write; `pg_dump -Fc`; deletes partial output on failure; executable bit confirmed in git | Not executed live (scope forbids container start/stop). |
| D4 | Restore script safely restores from a backup with explicit confirmation | IMPLEMENTED | scripts/restore.sh validates backup file + env-file + running service; requires a literal typed "yes"; `pg_restore --clean --if-exists --no-owner` | Not executed live (scope forbids container start/stop). |
| D5 | README setup/test instructions match the actual package.json scripts and repo layout | **BROKEN** *(refuted from IMPLEMENTED)* | README step 2 (`cp .env.example .env`) textually matches the client.ts runtime error message | **`.env.example` is not tracked in git at all** — confirmed 3 ways: `git ls-files --error-unmatch .env.example` fails ("pathspec did not match"); `git log --all -- .env.example` is empty (never committed, ever); a live `git clone` of this exact repo into a scratch directory produced only `.env.docker.example`. Root cause: `.gitignore`'s blanket `.env*` rule swallows the example file (unlike `.env.docker.example`, which was force-added). **A fresh clone cannot follow the README's own documented step 2** — `cp` fails immediately with "No such file or directory," blocking migrate/seed/dev entirely. |
| D6 | ARCHITECTURE.md's claims match the actual codebase | IMPLEMENTED | Stack table matches package.json; repository-layout tree matches `find` output on src/app and src/server; worker gap description matches docker-compose.yml's own comment | None. |
| D7 | DEPLOYMENT.md's steps match the actual Docker/backup tooling | IMPLEMENTED | Env-file/build/up steps match docker-compose.yml exactly; seed-in-Docker portability fix (`set -a; source .env.docker; set +a`) matches the corresponding commit | None. |
| D8 | .env.example documents every environment variable the running app reads | PARTIAL | (Duplicate of R1.54 at the "D" checklist letter.) 4 of 7 live-used vars documented | Same 2 undocumented vars as R1.54; compounded by D5 (the file documenting them isn't even in git). |
| D9 | No real secrets in the tree outside .env | IMPLEMENTED | Broadened secrets sweep (incl. AWS-key/private-key/sk-/ghp_ patterns): 0 real-secret hits; only tracked env file (`.env.docker.example`) contains the literal placeholder `change-me` | None. |
| D10 | Git history hygiene: clean, descriptive, no leaked artifacts | IMPLEMENTED | 76 commits, all genuinely descriptive (head-and-tail spot read); no build/runtime artifacts ever tracked; 1294 objects/6.1MB, no oversized blobs | None. |

## (e) Findings not tied to a single checklist item

### CRITICAL

1. **`completeInstallationAction` has no ownership/involvement check at all, and trusts client-supplied job-item IDs.** `src/server/appointments/complete-installation.ts:38-41` checks only `can(user, COMPLETE_INSTALLATION)` — no `isAppointmentAssignee` check (contrast `markAppointmentArrivedAction`, which does check). Line 98-103 does `UPDATE job_items SET status='installed' WHERE id IN (params.jobItemIds)` with **no `job_id = appointment.jobId` predicate at all**. Line 105-117 calls `recordCustomerPayment` against `appointment.jobId` with **no COLLECT_PAYMENT check**. Any seeded installer (Basel/Issam hold `complete_installation` with only `view_assigned_jobs`) who obtains any appointment UUID can complete an arbitrary appointment on an arbitrary job, mark job items from **any job** installed, advance that job to `installed`, and record a customer payment on a job they are not assigned to. This is the single worst-scoped mutation found in the codebase (worse than the other S1.3 findings because it combines missing job-scope, missing entity-scope, and missing financial-permission checks in one action).

2. **Login has zero rate limiting or lockout — live-demonstrated unlimited credential guessing against a real account.** `src/app/(public)/login/actions.ts:40-98` has no attempt counter, delay, CAPTCHA, or lockout anywhere. **Live Probe 2** sent 25 sequential wrong-password attempts against `+972501111111` (a real, active seeded user) and every one returned HTTP 200 with the identical generic error, at flat bcrypt-only latency (0.23-0.29s), with no 429 at any point; the account was confirmed `status='active'` after the flood. This is the S1.7/R1.48 gap made concrete with live proof against production-shaped data, not a hypothetical.

3. **`approveFactorySubmission`/`rejectFactorySubmission` trust the caller-supplied `jobId` instead of `request.jobId`, with no row lock.** `src/server/production/approve.ts:30-80` never compares the client-supplied `jobId` parameter (bound directly to a client button click, `factory-decision-buttons.tsx:134`) against the submission's real job. The `job_costs` INSERT and `advanceJobStatus` both use the raw parameter. An `APPROVE_FACTORY_PRICE` holder can book a real, approved factory cost — and advance job status — on a job they are not even involved in, simply by tampering with the action's arguments; the real request's own job is left "approved" with no cost row. Confirmed independently by the `auth-security`, `production-factory`, and `finance` domain agents, and this was the specific defect that caused the `production-factory` refuter to downgrade R1.29 from IMPLEMENTED to PARTIAL. No row lock also means two concurrent approvals can double-book the cost.

4. **Customer PII, including national ID, is retrievable by any user holding only `VIEW_CUSTOMERS` — with zero job-involvement scoping, unlike every other entity page in the app.** `src/app/(app)/customers/[id]/page.tsx:33-37` gates on `VIEW_CUSTOMERS` alone; `getCustomerById` (`customers/queries.ts:58-64`) has no ownership predicate at all. **Live Probe 1**, logged in as a real seeded technician (Basel) via the actual login form, opened the customer-detail page for a customer on a job he has zero relationship to and received a full 200 OK profile; a second probe against Ahmad's customer page returned the literal national ID string `300123456` in the page body. Reproduced independently against two more customers as a second low-privilege user (Issam). This is the customer-domain analog of finding #3 above and is the most severe of the several sale-price/PII exposures catalogued throughout this matrix (R1.15, S6.4, I5) because it exposes government-ID-grade PII, not just a price.

5. **Sale price and factory-submitted cost are rendered to every involved job viewer, including installers with zero financial permission — the single most-corroborated finding in this audit.** Independently found and confirmed by 5 of the 9 domain verifiers (auth-security, customers-jobs, production-factory, scheduling, and indirectly finance/dashboard via the reports pathway), tied to formal items R1.10, R1.41, P4, I5, S1.4, S1.5, S6.4, T4, RG13. Root cause is architectural: `ProductionSection` (`jobs/[id]/page.tsx:449-455`) and the job/jobs-list/customer-page sale-price renders are the only sections on the job page with no permission gate at all, while every other financial card (Costs, Compensation, Payments) is correctly gated. A single fix — gating `ProductionSection`'s price/history block on `VIEW_JOB_COSTS` and adding a dedicated sale-price permission everywhere `salePriceTotal` is rendered — would resolve the majority of the SECURITY ISSUE items in this matrix.

### HIGH

1. **`convertQuoteToJob` and `saveQuoteDraft`/`sendQuoteAction` never verify the quote belongs to the job, or that the caller may see the job.** `src/server/quotes/actions.ts:232-288` passes the caller-supplied `(jobId, quoteId)` pair straight to `applySignedQuoteToJob`, which deletes job Y's items and overwrites its sale price/quote pointer/closer with data from an unrelated signed quote X. `saveQuoteDraft`/`sendQuoteAction` have the same gap for creating versions and advancing status on foreign jobs.

2. **`deleteJobItem`, `removeAssignment`, `cancelAppointmentAction` all delete/mutate by row ID alone with no job-match check**, letting a `CREATE_PRICE`/scheduling-permission holder delete items or cancel appointments on jobs they cannot even open (`jobs/actions.ts:341-358,420-437`; `appointments/actions.ts:226-258`). Audit rows record the client-supplied `jobId` in every case, so the audit trail itself is poisoned.

3. **`createMeasurement` lets a restricted user self-escalate to full job visibility.** `jobs/actions.ts:173-181` checks only `CREATE_MEASUREMENT`; line 239-246 unconditionally sets `jobs.measuredByUserId = caller`, and `involvementFilter` treats that column as involvement everywhere (job page, jobs list, attachment route, quote PDF route). A `VIEW_ASSIGNED_JOBS`-only user who learns any job UUID (from a notification href, calendar payload, or shared link) can attach a measurement to it and thereby unlock the full job page, including sale price and factory cost.

4. **`/repairs` and `/customers` list pages leak company-wide data to users scoped to only their own jobs.** `/repairs` (`repairs/page.tsx:50-66`) gates on `CREATE_REPAIR` alone with no `restrictToUserId`; live-reproduced against Basel showing all 6 repairs including 2 jobs his own `/jobs/[id]` page correctly Forbids. `/customers` (`customers/page.tsx:33-40`) has no permission check at all.

5. **Self-approval of financial requests is possible everywhere and is silent** (formal items V1/V2, S1.6): no `decide`/`confirm` action in the codebase checks requester ≠ decider, and several auto-approve paths (`payments/record.ts`, `costs/actions.ts`, `compensation/actions.ts`) skip the approval queue entirely for the requester's own submission. DB shows 4 self-approved payments and 5 self-approved job costs already live in the seed data.

6. **The R1.33 installation-completion note is silently discarded, and the "photo taken" flag is not persisted anywhere queryable.** `complete-installation.ts:105-117` only writes `note` inside the `paymentCollected` branch (as the payment's own notes field) — a technician who completes without collecting payment loses whatever they typed; `photoTaken` exists only inside an `audit_logs` JSON blob, with no column on `appointments`/`jobs` and no display anywhere.

7. **Both `requirements-checklist.md` and `sprint0-checklist.md` (and a prior `coverage-synthesis.md`) referenced by this task's own instructions were not present in the scratchpad for 2 of the 9 domain agents** (`ui-localization-jobdetails`, `tests-deploy-docs-seed`), who had to reconstruct item wording and IDs from the task brief's own hints rather than the source-of-truth checklist text. Statuses/evidence for those ~29 items (R1.9-R1.11, R1.44, R1.45, R1.50, R2.2, S8.1-S8.2, S9.1-S9.3, R1.1-R1.4, R1.46-R1.47, R1.54-R1.58, P6, S10.1-S10.4, RG1-RG6, RG15, D1-D10) are independently well-evidenced from source/live testing, but their exact item-ID mapping against the *original* checklist could not be double-checked. The orchestrating process should confirm these files were actually generated for this sprint before treating this matrix as the final word on IDs.

### MEDIUM

1. **Re-signing a revised quote (v2) wipes and re-creates all `job_items`**, losing installed-status flags and item-linked assignments/costs (`quotes/convert.ts:51-63` unconditionally deletes then re-inserts with status `pending`); the existing factory request is not refreshed, so the factory keeps v1 specs after a v2 signature.
2. **Duplicate-phone customer creation crashes or surfaces raw English Postgres errors** in the office flows (`customers/actions.ts:77-88,132-143`, `jobs/actions.ts:86-99`) — only the field-measurement quick-submit path implements find-or-reuse; no `error.tsx` exists anywhere to catch the resulting unhandled exception gracefully.
3. **`/finance` shows every incoming/outgoing check to any `COLLECT_PAYMENT` holder** (`finance/checks-section.tsx:94-97`), including seeded installers who hold `collect_payment` but not `manage_checks` — company-wide check data (customer names, amounts, banks, payees) reaches a non-financial role.
4. **`cash_expense_reports` (the field-expense-report feature) has zero test coverage and zero seed data** — a fully-wired feature (schema + action + UI) that no automated suite and no demo dataset ever exercises.
5. **Cash drawers can be driven negative** — neither `createCashTransfer` nor `reportFieldExpenseAction`/`decideFieldExpenseAction` checks the sender's computed balance before posting a debit; `createCashTransfer` itself needs only a login, not `COLLECT_PAYMENT`.
6. **No manager-side action exists to record a company payout to a technician** — `payment_made` ledger rows can only be created by the technician's own self-report (`reportTechnicianPayment` hard-codes `userId: user.id`), so balances/earnings reports overstate what's owed whenever a worker forgets to self-report.
7. **A cleared incoming check never becomes a `customer_payment`** — `updateIncomingCheckStatusAction` only flips the check's own status; paid/remaining totals are computed solely from `customer_payments`, so check money either never reaches the customer's balance or must be separately double-entered.
8. **Auto-generated factory-request text interpolates a raw English unit key** (`meter`/`unit`) instead of the localized Arabic label the manual path uses, despite a doc comment claiming the two paths are mirrored.

### LOW

1. **`X-Powered-By: Next.js`** ships on every response (`next.config.ts` has no `poweredByHeader: false`) — minor stack-fingerprinting information disclosure, confirmed live by Probe 2.
2. **`generateMetadata()` leaks the real job number / customer identifier in the browser tab title** for jobs/customers the viewer is about to be shown Forbidden for (`jobs/[id]/page.tsx:54-62`, `customers/[id]/page.tsx:18-26`) — live-reproduced.
3. **Dashboard headline counters (customers/active-jobs/awaiting-approval) are company-wide** even for `VIEW_ASSIGNED_JOBS`-only viewers, inconsistent with the file's own scoping rule for every list below it (counts only, no names/amounts).
4. **Nav shows "المستخدمون والصلاحيات" to `MANAGE_PERMISSIONS`-only users who then hit Forbidden** — the admin pages require `MANAGE_USERS` too; that persona has no reachable UI to grant/revoke permissions.
5. **Stale `revalidatePath` targets** after finance mutations (`/technicians/[id]` instead of `/finance/technicians/[id]`; `/finance/cash`, `/finance/checks`, `/admin/permissions` — none of these routes exist) — users may see stale data until a manual reload.
6. **Session rows are never pruned** and IP is recorded from a spoofable `X-Forwarded-For` header with no trusted-proxy configuration.
7. **`quote_versions.notes` and `notes` on the PDF template are entirely dead** (declared in schema/type, never collected by any UI, never rendered) — see also formal items R1.20/R1.52.
8. **Global search omits address and quote-number**, and the existing btree indexes cannot serve the leading-wildcard `ILIKE '%term%'` queries actually issued (no `pg_trgm`/GIN index anywhere in the migrations).
9. **A stale, untracked git worktree** (`.claude/worktrees/charming-mayer-6a13b7/`) sits in the working tree — harmless (never committed, no real secrets) but dead weight that could confuse a future `git add -A`.
10. **`commission.ts`'s unique-violation recovery reads `err.code` instead of `err.cause.code`**, unlike every other module in the codebase that documents and correctly unwraps Drizzle's error-wrapping — the documented race-recovery path for concurrent commission estimate/finalize almost certainly never triggers.

## (f) Live probe log

Two live-probe sessions were run against the actual running dev server (`localhost:3000`) with real seeded credentials, using the real login form (not raw API calls) and psql (read-only) for ground truth. Every probe listed by each session is transcribed below in request → result form.

### Probe session 1 — "Installer/technician authorization boundary"

| # | Persona / Request | Result |
|---|---|---|
| 1 | auditor: read requirements-checklist.md / sprint0-checklist.md / coverage-synthesis.md | None of the 3 files exist in the scratchpad (only empty `probes/`/`toolchain/` dirs) — proceeded on the self-contained live-probe task. |
| 2 | auditor: read seed.ts + psql on users/permissions/jobs/customers | Basel (+972504444444) confirmed lowest-privilege: view_customers, view_assigned_jobs, create_measurement, complete_installation, collect_payment, add_fuel, create_repair (7 keys). All demo passwords: `password123`. |
| 3 | Basel: real login via the actual /login form | 302→200 to /dashboard, "مرحباً، باسل"; httpOnly `glass_session` cookie set. |
| 4 | Basel: GET /jobs | 200 — correctly scoped to "7 مهام"; uninvolved jobs (JOB-2026-0003, -0008) absent from the list. |
| 5 | Basel: GET /jobs/61ec77e1-... (JOB-2026-0003, not involved) | 200, Forbidden rendered ("هذه المهمة غير مسندة إليك"); raw body scanned for money/customer strings — 0 hits. Correctly blocked. |
| 6 | Basel: GET /jobs/a84167ee-... (JOB-2026-0008, not involved) | 200, Forbidden rendered; scanned for money/customer strings — 0 hits. Correctly blocked. |
| 7 | Basel: GET /jobs/00000000-... (nonexistent id) | 404 (Next.js not-found page). |
| 8 | Basel: GET /jobs/52360dcb-... (JOB-2026-0001, Basel IS involved; Costs/Compensation should be withheld) | 200 (95,850 chars), body scanned for cost/profit/commission figures — 0 hits. CostsSection/CompensationSection correctly omitted server-side, no RSC-payload leak. |
| 9 | Basel: GET /customers/56d28697-... (Ahmad, connected via Basel's own job) | 200 — page text includes phone and **national ID 300123456** in plain text. |
| 10 | Basel: GET /customers/0f058ca0-... (Khaled — customer on a job Basel has zero relationship to) | **200 OK, full 38,022-char profile returned — NOT Forbidden**, despite zero relationship to this customer or job. |
| 11 | Basel: GET /finance | 200, not Forbidden — shows only Basel's own drawer (5,000 ILS) and own history. Correctly self-scoped (COLLECT_PAYMENT-gated by design). |
| 12 | Basel: GET /finance/technicians (roster, needs VIEW_TECHNICIAN_BALANCES) | Redirected (opaqueredirect) to own ledger — correct self-service fallback. |
| 13 | Basel: GET /finance/technicians/<mohammad-id>, <issam-id>, <amr-id> (IDOR attempt) | All 3: 200 but Forbidden rendered ("يمكنك فقط الاطلاع على حسابك الخاص"). Correctly blocked. |
| 14 | Basel: GET /reports (no tab param) | 200, not Forbidden — only the "وقود المركبات" tab available via ADD_FUEL; other tabs correctly absent. |
| 15 | Basel: GET /reports?tab=vehicles | 200 — table shows fuel cost for **all 3 company vehicles**, including one (Mercedes) that is Issam's responsibility, not Basel's. |
| 16 | Basel: GET /api/reports/jobs, /customers, /technicians, /profitability, /vehicles (CSV) | jobs/customers/technicians/profitability → 403 (correct). **vehicles → 200 text/csv**, same all-vehicle data as the UI tab. |
| 17 | Basel: GET /api/notifications | 200, JSON correctly scoped to Basel's own userId. |
| 18 | Basel: GET /api/attachments/9605ba4a-... (attachment on a job Basel is involved in) | 200, image/png returned — expected (involved). No cross-user attachment could be tested live (only 1 attachment exists in seed data). |
| 19 | Basel: GET /api/quotes/f49a481b-.../pdf (Ahmad's quote, involved) vs /api/quotes/051779a0-.../pdf (Khaled's quote, not involved — IDOR attempt) | Ahmad's: 200, application/pdf, 93,277 bytes (expected). Khaled's: **403, correctly blocked.** |
| 20 | Basel: GET /approvals, /admin/users, /admin/audit-log, /settings | All 4: 200 but Forbidden rendered in every case ("لا تملك صلاحية الوصول"). Correctly blocked. |
| 21 | Basel: GET /my-day | 200, not Forbidden (expected — own daily view). |
| 22 | Issam: real login via /login (0503333333/password123) | 200 to /dashboard; dashboard's outstanding-balance widget correctly shows only 3 of 5 company-wide balances (jobs Issam is actually involved in) — re-confirms scoping for a 2nd account. |
| 23 | Issam: GET /customers/f3713193-... (Sara) and /customers/447bc5bf-... (Mona) — both uninvolved | **Both: 200 OK, full profile pages returned** (38,901 and 38,839 chars) — reproduces the same unscoped-customer-access pattern for a 2nd, independent low-privilege user. |
| 24 | Issam: GET /jobs/625f73a3-... (Sara's job, not involved) | 200, Forbidden correctly rendered — confirms the job-level IDOR protection holds even while the customer-level protection does not. |

### Probe session 2 — "Public tokens and abuse resistance"

| # | Persona / Request | Result |
|---|---|---|
| 1 | Prep: psql SELECT on quote_public_links / factory_public_links / users | 2 quote links (1 signed/exp 2026-09-19, 1 unsigned/exp 2026-09-15), 1 factory link (`expires_at` NULL, `revoked_at` NULL), 4 active users. |
| 2 | Anonymous: GET /public/q/bIK-AMub... (valid, already-signed quote) | 200, 23.9KB — quote number/version, line items, totals, signature confirmation, PDF link. No cross-customer data. |
| 3 | Anonymous: GET /public/q/9Go3mD7r... (valid, unsigned quote) | 200, 30.9KB — sign form prefilled with **that quote's own** customer name/phone/address, matching DB exactly. |
| 4 | Factory contact: GET /public/pr/NngN3yWS... (valid production request) | 200, 21.2KB — job number/title, item quantities for pricing. No customer PII/prices/attachments leaked. |
| 5 | Attacker: GET /public/q/not-a-real-token | 200, generic "الرابط غير صالح" notice; no stack trace, no path leakage. |
| 6 | Attacker: GET /public/q/<64 random hex chars> | 200, same generic invalid-link page. |
| 7 | Attacker: GET /public/q/ and /public/q (empty/base path) | 308 → /public/q, then 404 not-found. No crash. |
| 8 | Attacker: GET /public/q/<valid token>/ (trailing slash) | 308 redirect, then resolves normally — no bypass. |
| 9 | Attacker: GET /public/q/<percent-encoded valid token> | 200, resolves correctly. |
| 10 | Attacker: GET /api/public/quotes/<valid token>/pdf | 200, application/pdf, 88KB (headless-Chromium render). |
| 11 | Attacker: GET /api/public/quotes/<random token>/pdf | 404, `{"error":"الرابط غير صالح."}` — no leak. |
| 12 | Attacker: GET /public/q/%27%20OR%20%271%27%3D%271 (SQLi-style) | 200, generic invalid-link page — Drizzle's parameterized query unaffected. |
| 13 | Attacker: GET /public/q/<5000-char token> | 200, generic invalid-link page, no 500/timeout. |
| 14 | Attacker: GET /public/q/%00token and /public/q/..%2f..%2fetc%2fpasswd | Both 200, generic invalid-link page. No traversal, no crash. |
| 15 | Brute-force: 25× POST /login, phone=+972501111111 (real active user), distinct wrong password each time | **All 25 → HTTP 200, identical generic error, no 429 at any point, latency flat 0.23-0.29s (bcrypt-cost only, no backoff). Account confirmed `status='active'` after the flood.** |
| 16 | Flood: 25× rapid GET /public/q/bIK-AMub... (valid signed-quote token) | All 25 → 200, latency 0.044-0.097s (one 0.83s outlier), **no 429 at any point**; each view also updates `last_accessed_at` (expected side effect). |
| 17 | Baseline: token entropy + response headers | `generateSecureToken` = `randomBytes(32).toString('base64url')` = 256 bits (positive control — enumeration risk is entirely the *lack of rate limiting* on a *known* token, not guessability); no session cookie set on any /public/* route; `X-Powered-By: Next.js` present on all responses. |
